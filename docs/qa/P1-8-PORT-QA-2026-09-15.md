# BUGS

## 结论

**PASS（BUG=0）**。代码级端口、脚本、文档、品牌守卫和可执行回归均未发现本任务范围内缺陷；HTTP 临时端口因本窗口沙箱禁止 `bind` 未跑，不能推断为通过。

基线：`HEAD=f611824`；目标文件差异 `+69/-22`（8 文件）。工作树另有既存 `docs/model/DISPATCH-LOG.jsonl` 变更，本报告未改业务代码、未改 `docs/pm/`、未 commit/push。

## 七查逐项证据

### 1. Unit

- 端口真源：`app/server.py:54-68`。
- `V2O_PORT` 未设、空串、纯空格 → `PORT=8899`，rc=0。
- `V2O_PORT=8901`、`8899` → 分别 `PORT=8901/8899`，rc=0。
- `abc`、`8899.5` → rc=2，人话错误“必须是 1-65535 的整数”，无 traceback。
- `0`、`-1`、`70000` → rc=2，人话错误“超出 1-65535 范围”，无 traceback。
- 探针命令：逐项执行 `V2O_PORT=... python3 -c 'import app.server as s; print(s.PORT)'`。

### 2. Build

- `PYTHONPYCACHEPREFIX=/tmp/p18-pyc python3 -m py_compile app/server.py` → rc=0。
- `sh -n app/start.sh` → rc=0；`app/start.sh:3,8` 的 `set -eu` 与 `${V2O_PORT:-8899}` 不存在未定义风险。
- `app/index.html` 仅 1 个 inline script；抽取后 `node --check` → rc=0。
- `git diff --check` → rc=0。

### 3. Lint

- 本仓未发现 ruff/eslint 配置；专用 lint **未覆盖**，不推断为通过。
- 静态检查补充：目标文件 `8765` 零命中；目标 diff 为 `3/3, 3/3, 14/1, 8/5, 2/2, 2/2, 27/3, 10/3`，合计 `+69/-22`。

### 4. API

- 代码级真 handler＋HTTP 用例由 `tests/selftest_p1_2_contract.py:943-976` 执行到创建 `ThreadingHTTPServer`；首行已执行 `assert_tmp(root, ...)`，测试数据根为系统 tmp。
- 该用例在 `:949` 绑定 `127.0.0.1:0` 时被沙箱拒绝：`PermissionError: [Errno 1] Operation not permitted`，进程 rc=1。故临时端口 HTTP、handler HTTP 响应和“端口 !=8899”**未跑，不得推断为通过**。
- 未绑定端口的前置 handler 契约均输出 PASS；未访问外部项目占用的 8765，也未杀进程。

### 5. Logs

- 端口非法输入探针均为 rc=2＋中文 stderr，无裸 traceback。
- 代码路径：`app/server.py:62-67` 显式输出校验错误后 `SystemExit(2)`。
- 服务真实启动日志/API 5xx 路径因禁止 bind **未跑，不得推断为通过**。

### 6. Regression

- `python3 tests/selftest_p1_2_contract.py`：rc=1；已输出至 `7g` 的 PASS，随后在 `:949` 临时 bind 处受环境限制中止；P1-8 新牙口 `:960-975` 未执行到，**未跑，不得推断为通过**。
- `python3 tests/selftest_p1_2_frontend.py`：rc=0；`FRONT ALL PASS`、`FRONT SELFTEST PASS`。
- `python3 tests/selftest_v26_presets.py`：rc=0；`SELFTEST ALL PASS`。

### 7. DoD

- 默认端口=8899：**过**（`app/server.py:55-57`；未设 env 探针 PASS）。
- `V2O_PORT` 覆盖生效：**过**（`app/server.py:57-67`，8901 探针 PASS；`app/start.sh:8,30` 透传）。
- 两处默认值机械同步、改一处会红：**过**（`tests/selftest_p1_2_contract.py:962-971`；源码静态核对一致；牙口因 bind 限制未执行，已如实标注）。
- 文档无 8765 残留：**过**（8 个目标文件及 `app/server.py/start.sh` grep 零命中；README/usage/troubleshooting 均为 8899＋可覆盖措辞）。
- 历史 P2-4/P2-5“改一处忘一处”消除：**过（代码静态证据）**；`start.sh` 仅 `V2O_PORT:-8899` 一处默认值，URL 全部 `$PORT`（`app/start.sh:37-41`）。

## 品牌守卫独立复核

守卫实现：`tests/selftest_p1_2_frontend.py:1445-1450`，仅用 `\bV2O_PORT\b` 摘除技术令牌。

合成输入结果：精确 `V2O_PORT` → 免检；`V2OApp`、`V2O_PORT_EXTRA`、`XV2O_PORT`、`V2O 本机控制台` → 均红。实际 `app/start.sh` → PASS。粒度结论：不按子串放行，边界过窄/过宽问题未见。

## BUG 清单

| Bug ID | Priority | Stage P0 Blocking? | Repro | Status | Current Task | 备注（截图/日志一句） |
|---|---|---:|---|---|---|---|
| 无 | — | — | — | 无本任务范围 BUG | DEVELOP-P1-8 | API bind 限制是环境限制，不计 BUG |

## 未覆盖项

- 临时 HTTP/API：**未跑，不得推断为通过**；沙箱禁止本机 `bind`。
- contract P1-8 新牙口 `:960-975`：**未跑，不得推断为通过**；同一 bind 限制。
- ruff/eslint lint：**未跑，不得推断为通过**；仓库无配置。
- 真机 UI：本轮纯端口/脚本/文档改动，不适用；未启用真机 QA。

## Fix Attempt Fingerprint

- Task ID: `DEVELOP-P1-8`
- Root Cause Hypothesis: 默认端口与启动脚本曾存在多处硬编码。
- Approach: 独立环境矩阵、静态牙口、脚本语法、文档 grep、回归执行。
- Files Changed: 仅新增本 QA 报告。
- Verification: 见七查；无业务代码改动。
- Failure Reason: HTTP 临时端口受沙箱禁止 bind。
- Difference From Previous Attempt: 独立验证默认/覆盖/非法端口及品牌守卫合成坏例。

---

# supervisor 复检节（第三道，DEVELOP-P1-8 链收口前最后一道；以上正文一字未改）

- Task：**DEVELOP-P1-8「默认端口去硬编码：8765 → 8899」链收口复检**（放行 commit/push 前）
- Reviewer：supervisor（`opencode-go/muse-spark-1.3-contributor`，本窗口 subagent 直派）
- 基线：`HEAD=f611824867568d7d23f2e0a7ca6e6c7427b8a0af`（与 reviewer 首轮/返工同）；改动**未提交**，在工作树
- 取证目录（全部系统 tmp）：`/tmp/p18sup/`（`backup/` 8 文件＋`BASE.sha256`、`serve_probe.sh`、`port_matrix.py`、`falsify.py`、`negctl.py`、`recon.py`、`check_task.py`、`check_dispatch.py`、`*.out`）
- 本链打回计数：**0/2**（本次为 PASS，未打回）

## 一、裁定：**PASS，放行 commit／push `main`**

无 P0/P1/P2 阻塞项。三套自测与真起服务在生产路径上全跑通；qa 标注的三项「未覆盖」经复检全部定性（1 项环境限制已补位、1 项仓库无配置、1 项不适用），**不等于缺陷**。残留 P3×6 全部非默认路径或纯静态精度问题，挂 backlog。

## 二、三套自测实跑（本窗口可 bind，含 HTTP 段与新牙口）

| 套件 | 预期 | 实测 | rc | 关键输出 |
|---|---|---:|---:|---|
| `tests/selftest_p1_2_contract.py` | 517 | **517** | **0** | `SELFTEST ALL PASS（517 项断言）`，`^PASS` 行 517 |
| `tests/selftest_p1_2_frontend.py` | 167 | **167** | **0** | `FRONT ALL PASS` / `FRONT SELFTEST PASS` |
| `tests/selftest_v26_presets.py` | 58 | **58** | **0** | `SELFTEST ALL PASS` |

**qa「未覆盖」三条定性（此处补位）**：

1. **HTTP 段与 P1-8 新牙口——判为环境限制，已补位。** qa 的 `PermissionError: [Errno 1] Operation not permitted`（`tests/selftest_p1_2_contract.py:949` 临时端口 bind）是 codex 沙箱限制，不是代码问题。本窗口实跑到 `:949` 之后：`part8_http_contract` 真 loopback 全过，且 P1-8 新牙口逐条实际执行并通过（输出 `contract.out:268-273` 六行）：
   - `:268` `PASS 8 端口不是 8899（不碰线上服务）`
   - `:269` `PASS 8 未设 V2O_PORT 时默认端口＝8899（真源，防回退）`
   - `:270` `PASS 8 start.sh 端口收敛成 V2O_PORT:-<默认> 单变量（恰好一处）`
   - `:271` `PASS 8 start.sh 默认值＝server.py 默认端口（真源分叉即挂）`
   - `:272` `PASS 8 start.sh 除该默认值外无裸端口数字（防再写死一份）`
   - `:273` `PASS 8 start.sh URL 全走 $PORT（非注释行无 127.0.0.1:<数字> 硬编码）`
2. **lint——判为仓库无配置（非本链缺陷）。** Glob `pyproject.toml/setup.cfg/ruff.toml/.ruff.toml/tox.ini/.eslintrc*/eslint.config.js/package.json` **零命中**；无配置即无「lint 该过而未过」的可判基准。
3. **真机 UI——不适用。** 本链交付面为端口＋启动脚本＋文档，无 UI 改动（`app/index.html` 零改，见 §五）。

## 三、真起服务验证（生产路径）

| 用例 | 起法 | 实测监听 | curl | 停止后 |
|---|---|---|---|---|
| A 默认 | `python3 app/server.py`（未设 `V2O_PORT`） | `127.0.0.1:**8899**`（PID 87686，本项目自起） | `http://127.0.0.1:8899/` → **HTTP 200** | 8899 监听**已空** |
| B 覆盖 | `V2O_PORT=8903 python3 app/server.py` | `127.0.0.1:**8903**`（PID 88224） | `http://127.0.0.1:8903/` → **HTTP 200** | 8903 监听**已空** |

- **两份自起服务均已停**，未留残留进程（收尾 `lsof`：8899/8903 皆空）。
- **外部 8765 零扰动**：全程**未 bind 8765、未 kill PID 6586**（无权限需求也未请求）；复检开始与结束时 `lsof -nP -iTCP:8765 -sTCP:LISTEN` 均为同一 **PID 6586**（`python3.1`，属另一无关项目）。

## 四、端口环境矩阵自查（13 例，子进程隔离实测）

| `V2O_PORT` | 实测 | 判定 |
|---|---|---|
| 未设 | `PORT=8899` rc0 | 默认正确 |
| `""`（空串） | `PORT=8899` rc0 | 回落正确 |
| `"   "`（纯空格） | `PORT=8899` rc0 | 回落正确（`.strip()`） |
| `8901` / `8899` | `PORT=8901` / `8899` rc0 | 覆盖正确 |
| `65535` / `1` | `PORT=65535` / `1` rc0 | 边界接受 |
| `abc` | rc**2** ＋人话「必须是 1-65535 的整数」 | 无裸 traceback |
| `8899.5` | rc**2** ＋同上 | 无裸 traceback |
| `0` / `-1` / `70000` | rc**2** ＋人话「超出 1-65535 范围」 | 无静默钳制 |
| `" 8899 "` | `PORT=8899` rc0 | `int()` 容错，无害 |

**判定：默认路径（未设/空串/纯空格）无坑。** 仅两处**非默认路径**残留（本轮独立复现，与 reviewer P3-1／P3-2 一致，维持 P3 不升）：

- **P3-1 复现**：`V2O_PORT='   ' sh -c 'PORT="${V2O_PORT:-8899}"; echo "[$PORT] URL=[http://127.0.0.1:$PORT/]"'` → `URL=[http://127.0.0.1:   /]`（服务实监听 8899，仅 start.sh 打印/`open` 的 URL 变坏）。默认路径不触发。
- **P3-2 复现**：本机 `bind(('127.0.0.1',1))` → `PermissionError [Errno 13]`，即 1-1023 解析通过后 bind 抛裸 traceback；消息自称 1-65535 与实测能力不符。默认路径不触发。

## 五、反向证伪（6 条，逐条 /tmp 备份还原；**未用 `git checkout`**）

| 编号 | 改坏内容 | 跑什么 | rc | 咬住的断言（原文摘要） | 牙口 |
|---|---|---|---|---|---|
| F1 | `app/start.sh:8` `V2O_PORT:-8899` → `:-8888` | contract | **1** | `FAIL 8 start.sh 默认值＝server.py 默认端口…<< ['8888']` ＋`FAIL …无裸端口数字…<< ['8888','8899']` | **有牙** |
| F2 | `app/server.py:56` `PORT = 8899` → `8765` | contract | **1** | `FAIL 8 未设 V2O_PORT 时默认端口＝8899（真源，防回退）<< 8765` | **有牙**（钉值） |
| F3 | `app/start.sh` 追加 `echo "V2OApp 本机控制台"` | frontend | **1** | `AssertionError: start.sh 可见文案 V2O 残留（仅豁免 V2O_PORT 令牌）` | **有牙** |
| F4 | `app/start.sh` 追加 `V2O_PORT_EXTRA=1` | frontend | **1** | 同上（令牌边界生效，前缀不再豁免） | **有牙** |
| F5 | `app/start.sh` 三处 URL → 硬编码 `127.0.0.1:8765` | contract | **1** | `FAIL …无裸端口数字…<< ['8765','8899']` ＋`FAIL …URL 全走 $PORT…`（:37/:39/:41 三行列出） | **有牙**（首轮无牙项已闭） |
| F6 | **只改** `app/server.py` → `8888`（start.sh 不动） | contract | **1** | `FAIL 8 未设 V2O_PORT 时默认端口＝8899…<< 8888` ＋`FAIL …跨文件同步…<< ['8899']` | **有牙**（双向成立） |

- **还原**：6 条全部从 `/tmp/p18sup/backup/` `cp` 回写，**禁 `git checkout`**；每例后 8 文件 sha256 全量核对＝**逐字一致**（6/6），末次 `shasum -a 256 -c /tmp/p18sup/BASE.sha256` → **8/8 OK**。
- 复检前后工作树 `git status --porcelain` 与 `git diff --numstat` **逐字相同**：`8 文件 +69/−22`（3/3、3/3、14/1、8/5、2/2、2/2、27/3、10/3）＋`docs/model/DISPATCH-LOG.jsonl 4/0`（TM 记账，非本链业务）。**探针零入仓。**

## 六、两账本校验（第二道 schema ＋ 负控，只看 exit 码）

| 文件 | 行数 | 第二道校验 exit | 负控 |
|---|---:|---:|---|
| `docs/model/TASK-MODEL-LOG.jsonl` | **54** | **0** | 8 例坏例（JSON 坏／缺 `task`／`cost_cny` 缺键／`result` 枚举错与缺失／`escalated` 枚举错／`rework` 为 bool／为字符串）**8/8 exit=1 有牙** |
| `docs/model/DISPATCH-LOG.jsonl` | **84** | **0** | 7 例坏例（JSON 坏／缺 `date`／`note` 缺键／`used`≠主／`used` 缺失／`runtime` 枚举错／`result` 枚举错）**7/7 exit=1 有牙** |

- 正控：合法行两文件各 **exit=0**（校验器不误伤）。
- 校验逻辑照 `docs/roles/supervisor.md` 卡面整块照粘（8 键＋`used` 恒「主」＋`result`／`runtime` 枚举），路径写 `/tmp/p18sup/check_*.py` 以支持负控复用。

## 七、三处对账（override 表 vs DISPATCH.used vs 本链实派）

- **override 表**：builder＝`opencode-go/deepseek-v4.1-flash`、code-reviewer＝`opencode/muse-spark-1.3-contributor-free`、qa＝`codex/gpt-5.6-luna`、supervisor＝`opencode-go/muse-spark-1.3-contributor`（本窗口）。
- **本链 5 派 vs DISPATCH 落盘**：`L81` builder 首版 ✅ ／ `L82` builder 补两条 ✅ ／ `L83` reviewer 首轮 ✅ ／ `L84` reviewer 返工复核 ✅ ／ **qa ⛔ 缺行** → **本链缺行 1**，且 4 行 `used` 全「主」、`model` 与表逐字一致。
- **全量 84 行**：`model` 与 override 表**不匹配 2 处**，均为**表换代前历史行**（`L1` 迁移整理 supervisor 记 `deepseek-v4.1-flash`、`L8` P0-1 builder 记 `codex/gpt-5.6-luna`）——与 P1-6 复检同一现象，**非本链、不阻塞**，沿例不改历史行。
- **需 TM 补办（不阻断放行）**：`DISPATCH-LOG` 补 `qa` 行（本链第 5 派，`codex/gpt-5.6-luna`／`codex`／PASS）；`TASK-MODEL-LOG` 补本链任务行（`rework=0`、`escalated=NO`）。

## 八、红线核查

| 红线 | 取证 | 结论 |
|---|---|---|
| 未碰 secrets | 8 文件 diff 扫 `api[_-]?key/secret/password/sk-/bearer` → 零命中 | PASS |
| 未碰封存物 | `ls -d V1.10 V2.0` → 均不存在 | PASS |
| 未碰 `008林粒粒AI编程/` | `git status --porcelain -- "008林粒粒AI编程/"` → 空 | PASS |
| 未越界改受保护区 | `git status --porcelain -- app/index.html src/ docs/pm docs/handoff` → **全空** | PASS |
| 测试只用外置 tmp＋合成数据 | `contract:944` `assert_tmp(root,"part8_http_contract")` 在真 handler 前；`:952-961` 只动 `os.environ`＋重载模块无写盘；本窗口真起服务数据根＝系统 tmp（`/tmp/v2o-console-data` 起后**不存在**＝无落盘） | PASS |
| 用户真实视频目录／Obsidian 库零写 | 探针只起本项目 `app/server.py`，未访问也未写入 `Downloads/需转录视频` 与 ob 库 | PASS |
| 外部 8765 零扰动 | 全程未 bind／未 kill；前后同一 **PID 6586** | PASS |
| 只读业务代码 | 只读 `app/`、`tests/`、`README*`、`docs/usage.md`、`docs/troubleshooting.md`；唯一写盘＝本报告尾部追加本节＋`/tmp/p18sup/*` | PASS |

## 九、残留（全部非阻塞，挂 backlog）

- reviewer 残留 P3×6 经本轮抽样复现**属实**：P3-1（start.sh 纯空格 URL）、P3-2（<1024 裸 traceback）两项本轮独立复现；P3-4（numstat 口径，以实测 +69/−22 为准）、P3-5（`HANDOFF.md:73/:46` 与 `docs/pm/PRODUCT_PLAN.md:11` 的 8765／旧变量名 `PORT=`，**属 TM 与 planner 域，本窗口禁改**）、P3-6（`contract:968-970` 全文扫裸端口数字会误挂注释里的年份/期号）三项未复跑，采信 reviewer 取证。
- 本链未覆盖（沿用并不因复检改变）：真机浏览器目检、Windows 侧启动入口、8899 被占用时 `start.sh` 的提示质量（归 P1-1 或 backlog）。

**最终裁定：PASS——可 commit／push `main`。** 本链打回计数维持 **0/2**，无需升 senior-expert。

