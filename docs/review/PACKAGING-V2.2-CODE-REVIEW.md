
# CODE REVIEW

- Task: 复核 app/ V2.2（启动门作用域隔离 monkeypatch 是否恢复/归属解不出 fail-closed、clear 级联是否删错他 input/src 零碰、note 接口截断、两链接编码）
- Commit: N/A（非 git 仓库；评审对象为工作区现状 app/server.py 1931行 / app/index.html 882行 / app/start.sh 40行；src/ 只读对照 src/stage2/store.py 331行 + instance.py 213行 + stage5/startup.py 237行）
- Reviewer: code-reviewer（opencode-go/glm-5.3-flash）
- Result: 打回+改法（P0×1 必修：_launch input_abs NameError 致启动必炸；P1×2 必修：归属 fail-closed 洞 + 前端 file:// 未编码；修完可过）

> Dispatch / Evidence ID 系字段 2.0 已废弃，不填。

## P0 / P1 Findings

- P0-1 _launch 引用未定义 input_abs，启动必炸（monkeypatch 恢复无从谈起）：server.py:1391 签名 `(data_root, input_root, ob_vault_root, profile_hash)`，但 1404 `_install_scoped_gates(input_abs)`。全局 grep `input_abs` 在 app/server.py 仅此一处，无全局/闭包定义；AST + `inspect.getsource` 实证。后果：进 `_launch` 即 `NameError`，外层 `except` 转 `_humanize_startup_error` 通用分支“启动监听失败：name 'input_abs' is not defined…”，`_listener.running=False`，`run_startup` 从未执行，V2.2 全部作用域隔离逻辑死代码。且 `NameError` 发生在内层 `try/finally` 之前，`_restore_scoped_gates` 未执行（本次 scope 仍 None 无污染，但门从未装过）。改法：一行：`_install_scoped_gates(input_root)`（或 `os.path.abspath(input_root)`；`_is_under_root` 内已 realpath，保持与 `_scoped_source_ids` 同一归一口径即可）。回归：修后 `py_compile` + 启动一次看 `running=True`，再做他 input 历史行不卡门/同目录重入仍 BLOCK 两例。
- P0-1 附带（恢复逻辑本身 PASS，待 P0 修后生效）：`_install/_restore`（309-333）保存原函数一次、赋值 scoped、inner-finally 必恢复、scope 清 None，已实测装→patch→恢复→重装两轮 `is` 一致；`instance.recovery_bootstrap`（instance.py:143-144）走 `store.assert_*` 模块属性，patch 可见（`stage2/__init__.py` 的 from-import 绑定未被启动链使用，不影响）；异常文本与 src 原版逐字一致（“Stage3+ table not empty…”/“Run in transcription-stage status…”），`_humanize_startup_error`（352-369）二分支可命中，`GATE_STAGE3_BLOCKED/GATE_RUN_STATE_BLOCKED + _suggest_data_root` 链完整。`_ORIG_ASSERTS` 恢复后不清字典系有意复用（见 P2-1），不算错。
- P1-1 归属解不出未完全 fail-closed（注释 148/265 声称“过滤链上任何解不出的一律计挡住”，实现漏一档）：`_blocked_run` 对 `rid None / run2src miss` 回 True、`archive source NULL` 计挡住、`except→orig(con)` 回严门，三处是对的；但 `sid` 存在而其 source 行路径解不出（`current_path=''` + `pik=''` → 不在 `scoped`）时，两门均当“他 input”放行。实测（内存库）：hole source（空路径）+ `TRANSCRIBING` run，真原门 BLOCK（`{'TRANSCRIBING':1}`），scoped 门回 `{}` PASS；hole run 挂 norm（`raw_run_hole`）时 scoped stage3 回全 0 PASS，原门必 BLOCK。成因：`_scoped_assert_no_transcription_states:294` 条件 `sid in scoped or sid is None or sid not in all_srcs` 对“在 all_srcs 但不在 scoped”的 hole 取 False 直接跳过；stage3 `_blocked_run:200` 对 `src not in scoped` 同理。现实可达性低（DDL `sources.current_path NOT NULL` + 正常链恒写非空，空串需手造），故定 P1 非 P0。改法（二选一）：① runstates 分支加 `or sid not in scoped_with_unknown` 即把“source 行存在但 `_scoped_source_ids` 不含且 `all_srcs` 含”判 BLOCK（需另查该 sid 的 cur/pik 是否为空/不可判定）；最简：`scoped_plus_unknown` 外再算 `undecidable={sid | cur/pik 均空或均不在任何 input 下}`，`hist` 条件加 `or sid in undecidable`；② stage3 `_blocked_run` 同理：`src in scoped→False（放行），src is None→True，src not in all_srcs→True，src in undecidable→True`。另 `raw_artifact_id` 非 `raw_` 前缀即 BLOCK（222-228）系过严 fail-closed（脏数据永久卡新目录），建议保持（安全方向）但加注释说明。
- P1-2 两链接编码前后端分裂：后端对（`_finder_url:783-785` `quote(abspath,safe="/:")`、`_obsidian_url:788-811` realpath+commonpath+`quote(vault,safe="")+quote(rel,safe="")`）实测 `/tmp/带 空格/视频 #1?.mp4→file:///tmp/%E5%B8%A6%20…%231%3F.mp4`、`vault My Vault→obsidian://open?vault=My%20Vault&file=sub%2Fdir%2F…` 正确；但前端 `outCellFor:326` 与 `renderOpenRow:442` 均 `'<a href="file://'+esc(out)+'"'`，`esc` 仅转 `&<>"`，空格/中文/`#/?/%` 原样进 href，`#/?` 直接截断/变 query（`a #1?.mp4` 必断），与后端已编码 URL 不一致。且 `/api/note` 回的 `finder_url/ob_url`（1846-1851）在 `loadNote:452-470` 从未被渲染（只用 `o.path/o.text/o.stage_text`），后端正确编码死代码。OB 侧前后端一致（均 `%2F` 编码 `/`，Obsidian 解码可用，不算错；前端 `obLinkFor:313-322` 字符串前缀判与后端 realpath 判有 symlink/trailing-slash 差异，P2）。改法：前端两处 `file://` 改 `file://"+encodeURI(out).replace(/#/g,"%23").replace(/\?/g,"%3F")` 或复用后端：`loadNote/renderOpenRow` 改调 `/api/note` 的 `finder_url/ob_url` 直链（推荐后者，单源）；至少给 `renderOpenRow` 加 `encodeURI`。回归：带空格+`#?`+中文三例点“在访达中打开”不断链。
- P1-3 clear 级联主体 PASS（未删错他 input，src 零碰）：实测同 data_root 下 A/B 各 1 source/run/cand/norm/artifact+jobs，`_handle_clear_post({data_root,inputA})` 回 `cleared{1/1/1/1/1/job_dirs:1}`，B 侧 source/run/norm/jobs 全留，`jobs/<runA>` 单层删（1778 `abspath.startswith(jobs_base+sep)` 防穿越）。删除面：sources/runs/candidates/artifacts/norm链（`raw_<run_id>` 逐级）/rend/pub/state_events（`entity_id IN`）/archive/sources 顺序子先父后（FK ON 下 runs/artifacts/candidates/archive 先于 sources，正确）；只读 `open(w)` 仅 manifest/raw/md，`shutil.rmtree` 仅 `data/jobs/<run_id>`，`rg stage.*_.*` 无 `from stageX import _*` 私有导入，`src/` 无写。P1 尾巴两条：① 清空时不拦运行中（`btnClear` 常驻，`_handle_clear_post` 无 running 锁，worker 持 con 长事务时 `BEGIN IMMEDIATE` 顶 30s busy 后才 500，前端双 confirm 文案未提示“监听中清空会与 worker  race”）；改法：运行中点清空要么 409 拒（推荐，附“先停止再清空”），要么文档注明；② `archive_commits source NULL` 行清不掉（1748-1750 `WHERE source_id IN` 过滤 NULL）但 scoped 门 `n_arch` 把 NULL 计 BLOCK（253），NULL 脏行会致“清完仍 BLOCK”；现实 NULL  arch 需手造（正常 commit 恒带 source），定 P1 尾巴：改法：clear 加一句 `DELETE FROM archive_commits WHERE source_id IS NULL`（仅当清的是该 data_root 全库无归属孤儿时；或门侧 NULL 改按“他 input 忽略”，二选一并注释，两边须一致）。

## P2 / P3 Backlog Findings

- P2-1 `_ORIG_ASSERTS` 恢复后不清字典（322-333 只恢复赋值不清键）：复用正确（第二轮 `_install` 跳过保存沿用首轮 orig，实测两轮 `is` 一致），但模块热重载/单测 monkeypatch 换 `store` 对象时会拿 stale orig。改法：`_restore` 尾加注释“不清字典系有意复用；热重载测试需手动 `clear()`”，或恢复后 `pop` 并在 `_install` 每次重存（重存须在赋值前，当前 `if not in` 守卫要同步改）。
- P2-2 `NOTE_MAX_CHARS=6000` 截断语义 PASS 但实现糙：1840-1844 `len(full)` 按字符计、`full[:6000]+截断注明（全文 N 字）` + `truncated/total_chars` 三件套，前端 `<pre>+esc()` 无 XSS；`FAIL` 无 md 回阶段人话（`_stage_text_zh` 覆盖 发现/听写整理成稿入库五段 + FAILED_RETRYABLE/NO_SPEECH_DETECTED + 排队兜底）正确。糙处：`open(md).read()` 全量进内存（长视频 md 数 MB 级可接受，超大无流式，上限仅前端 40vh 滚动），`OSError` 分支 `total_chars:0` 与已读长度不一致（小）。改法：注明“按字符非字节”，超大文件改分块读前 6000+1 字符判截断。
- P2-3 `_handle_clear_post:1642` `src_rows` 死查询（查后不用，`path_rows` 重查一次），`cand_ids` 对 `source_id IN scope` 或 `pik under root` 双判正确（他 input 不动），`state_events` 按 `entity_id IN` 不带 `entity_type`（UUID 碰撞可忽略，前缀命名空间已隔离）。改法：删 `src_rows` 三行，`state_events` 删前加注释“按 ID 全类型删，跨类型同值碰撞概率忽略（uuid12hex）”。
- P2-4 `index.html` `esc()` 未转单引号（V2.1 P3-1 延续）：现拼接位为双引号属性 + 文本节点，无实际 XSS；拼单引号属性前再补 `'`→`&#39;`。
- P2-5 打包/启停延续（V2.1 P2-5）：`start.sh sleep 1+kill -0` 慢机误判、`PORT 8765` 两边硬编码、`started_at` 置位与 `_launch` 隔线程窗（V2.1 P2-4 延续），本轮未动，不卡 V2.2。
- P2-6 `_listener_snapshot` 三把锁非原子（V2.1 P2-6 延续）+ `threading.Event().wait(0.1)×50`（V2.1 P3-2 延续）+ `_worker_done` 无界（V2.1 P3-3 延续），本轮未动。
- P3-1 `sources.current_path NOT NULL` 使 `_scoped_source_ids:169-171` 的 `not cur and pik` 分支与 runstates `sid is None` 分支为防御死代码（`processing_runs.source_id NOT NULL + FK` 使孤儿/NULL 仅手造可达），保留无害，建议加注释“正常链不可达，手造脏数据时 fail-closed”。

证据备注：`py_compile app/server.py` 过；`input_abs` 全局唯一引用 1404 行；scoped A/B/C 三例（A BLOCK 1 行 / C PASS 0 行 / 坏 raw BLOCK）+ hole 双例（runstates `{}` PASS vs 真原门 BLOCK；hole-norm 全 0 PASS）+ clear A/B 隔离例 + finder/obsidian 编码对比例均本机实跑；`src/stage2/store.py:49-51 ALLOWED + 298-321` 双门原文已核，`instance.py:143-144 store.*` 属性调用确认 patch 通道有效。
