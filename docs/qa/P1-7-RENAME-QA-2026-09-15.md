# BUGS

## 结论

**PASS（P1-7 改名代码级独立 QA；无 P0/P1）。**

页面、README、菜单栏、CLI 与启动脚本的用户可见品牌已统一为「懒得笔记」；兼容性禁改项保持原样。未启动真机浏览器、未启动 8765 服务。contract 自测因当前执行环境禁止绑定 localhost 随机端口而 rc=1，已作为 P2 环境限制如实记录，不能视为业务断言通过。

## 测试摘要

| 检查 | 结果 | 证据 |
|---|---|---|
| 独立坏例探针 | PASS | 自写 Python 扫描六个目标文件；用户可见旧品牌无残留。`.v2o` CSS 类、`v2o-*` 键、README 兼容括注、`V2OApp` 类名按任务书豁免。 |
| contract 自测 | NOT_VERIFIED（环境限制） | `python3 tests/selftest_p1_2_contract.py`，rc=1；前置 245 项 PASS，随后在 `tests/selftest_p1_2_contract.py:949` 创建 `ThreadingHTTPServer` 时触发 `PermissionError: [Errno 1] Operation not permitted`，未出现业务断言 FAIL。 |
| frontend 自测 | PASS | `python3 tests/selftest_p1_2_frontend.py`，157/157 PASS，rc=0；含 `BRAND SELFTEST PASS`。 |
| presets 自测 | PASS | `python3 tests/selftest_v26_presets.py`，58/58 PASS，rc=0。 |
| 断言有牙变异 | PASS | 临时将 `app/index.html:6` title 改回 V2O，frontend rc=1，命中 `title 品牌回退`；恢复后 frontend rc=0。 |
| 变异还原完整性 | PASS | `/tmp` 备份/还原，变异前后 `app/index.html` SHA-256 均为 `954d54d09118f045d0268b891e4469d6df4352b8bad94232a127649130c3a6b0`。 |
| diff 空白 | PASS | `git diff --check` rc=0。 |
| 真机/服务 | NOT_VERIFIED（按任务要求） | 未打开浏览器，未启动 8765；title/h1 仅做源码与自测核验。 |

## 用户可见品牌逐处核验

| 文件 | 证据 | 判定 |
|---|---|---|
| `app/index.html` | `:6` title；`:38` 磁带标识；`:179` h1；`:1060/:1064` 通知标题均为「懒得笔记」 | PASS |
| `README.md` | `:1` 标题、`:11` 首句为「懒得笔记」 | PASS |
| `README.en.md` | `:1` 标题、`:11` 首句为「懒得笔记」；`:1/:11` 的 `Video2Obsidian` 是仓库/兼容括注，正确保留 | PASS |
| `src/stage12/menu_bar.py` | `:47-48` 状态标题、`:92` rumps 应用名、`:139` help 描述均为「懒得笔记」；`:88/:118/:160` `V2OApp` 是类名/引用，豁免 | PASS |
| `src/stage12/status_cli.py` | `:124-125` help 描述为「懒得笔记」；`:123` `prog="stage12-status"` 是技术入口标识，豁免 | PASS |
| `app/start.sh` | `:2` 注释、`:26` echo 均含「懒得笔记」；“本机控制台”是功能描述，不是旧品牌 | PASS |

## 交叉一致性

- README 中文标题 `README.md:1`、英文标题 `README.en.md:1`、页面 title `app/index.html:6`、页面 h1 `app/index.html:179` 的主品牌均为「懒得笔记」。
- 菜单栏 rumps 应用名 `src/stage12/menu_bar.py:92`、状态标题 `:47-48`、CLI 描述 `src/stage12/status_cli.py:124-125` 与页面通知标题 `app/index.html:1060/:1064` 一致使用「懒得笔记」。

## 禁改边界独立核

| 禁改项 | 证据 | 判定 |
|---|---|---|
| `app/server.py` 零改动 | `git diff --numstat -- app/server.py` 无输出 | PASS |
| 五个 `v2o-*` 键 | `app/index.html:326` 的 `v2o-input-root`、`v2o-vault-root`、`v2o-data-root`；`:352/:357/:359/:360/:364` 的 `v2o-theme`、`v2o-theme-migrated` | PASS |
| 默认浅色 | `app/index.html:2` `data-theme="light"`；`:17` light 主题；`:364` light 兜底 | PASS |
| `.v2o` class | `app/index.html:27` CSS、`:179` class 原样保留 | PASS |
| 数据目录名 | `app/server.py:53` `v2o-console-data` 原样保留 | PASS |
| 日志前缀 | `app/server.py:6051` `v2o-console:` 原样保留；server.py 零 diff | PASS |
| 版本串 | `app/server.py:6048` `V2OConsole/1.1` 原样保留 | PASS |
| GitHub 仓库名 | `origin` 仍为 `wanghoufan/Video2Obsidian.git` | PASS |

## BUG 清单

| Bug ID | Priority | Status | Repro / 证据 | 备注 |
|---|---|---|---|---|
| QA-P17-001 | P2 | OPEN（环境限制） | `python3 tests/selftest_p1_2_contract.py` 在 `:949` 绑定 `127.0.0.1:0` 时 rc=1，异常为 `PermissionError: [Errno 1] Operation not permitted` | 当前沙箱网络能力限制；不是改名业务断言失败。需在允许本机 loopback 的环境补跑确认 contract rc=0。 |

## P0 / P1 Findings

无。

## P2 / P3 Findings

- **QA-P17-001 / P2**：contract 自测的 HTTP 段在当前环境无法绑定 localhost 随机端口，属于验证环境限制；本轮已有 245 项 contract 断言 PASS，不能替代完整 contract 通过结论。

## 未覆盖项

- 真机浏览器 title/h1 渲染、像素布局、通知弹窗：NOT_VERIFIED（任务书明确禁止）。
- 8765 服务启动与真实 HTTP 旅程：未执行（任务书明确禁止；contract 的随机端口段受环境限制）。

## Fix Attempt Fingerprint

- Task ID：`DEVELOP-P1-7`「产品改名懒得笔记」独立 QA
- QA 角色：qa
- 工作树基线：HEAD `7bc8cec`，存在任务交付未提交改动；本轮未修改业务代码、测试代码或既有文件。
- 独立取证：自写 Python 品牌探针、源码行号核验、三套自测、临时 title 变异、SHA-256 还原复验、git diff 边界核。
- 外置数据：变异输出与备份均在 `/tmp`；未写真实视频目录、Obsidian 库或仓库 `data/`。
- Git：未 commit、未 push。

心跳：目标＝完成 P1-7 改名独立 QA｜剩 P0＝0｜下一步＝提交本报告给 supervisor 复检。

---

# supervisor 复检（2026-09-15 13:27）＝PASS／无 blocking／本链 0/2

复检人：supervisor（独立取证，不采信 QA/复核自报；HEAD=7bc8cec，工作树 P1-7 未提交交付属本链交付物）。

## 1. 三处对账（0 不匹配）

HANDOFF「DEVELOP-P1-7 全链」节 6 派 ↔ `DISPATCH-LOG` L70-75 ↔ `TASK-MODEL-LOG` L53，逐行核对：

- builder×3（首版／返工／微修）＝`opencode-go/deepseek-v4.1-flash`、runtime 本窗口 — 与根分工表 builder 行逐字一致。
- code-reviewer×2（首轮／返工复核二）＝`opencode/muse-spark-1.3-contributor-free`、runtime 本窗口 — 与表逐字一致。
- qa×1＝`codex/gpt-5.6-luna`、runtime codex — 与表逐字一致。
- 全部 `used=主`、`result=PASS`；TASK L53 字段齐全、枚举正确、`rework=0` 与本链 supervisor 打回 0/2 口径一致、`escalated=NO`。HANDOFF ③（TM 裁定）为编排动作非派工，不计行，正确。

## 2. 两账本校验（exit 0 ＋ 负控 13/13 有牙）

- 角色卡第二道校验器实跑：TASK=53 行 exit 0、DISPATCH=75 行 exit 0。
- 坏行负控（/tmp 副本，不动真账本）：TASK 7 类（JSON 坏／缺键／result 枚举错／escalated 枚举错／rework 非整／rework 布尔／_example 豁免）＋DISPATCH 6 类（JSON 坏／缺键／used 非主／result 枚举错／runtime 枚举错／_example 豁免），坏例全 rc=1、豁免例 rc=0，13/13 符合预期。验后 /tmp 副本弃用，真账本未动。

## 3. 三套自测实跑（全 rc=0）

- `tests/selftest_p1_2_contract.py` → **rc=0，482 项 ALL PASS**（本窗口 HTTP 段成功绑定本机端口，无 PermissionError）。
- `tests/selftest_p1_2_frontend.py` → rc=0，157/157，含 `BRAND SELFTEST PASS`。
- `tests/selftest_v26_presets.py` → rc=0，58/58。

## 4. 独立品牌探针（/tmp 自写脚本，六文件全过）

- 落点逐处核验：index.html :6/:38/:179/:1060/:1064、README.md:1、README.en.md:1、menu_bar.py:92、status_cli.py:124、start.sh:26，全部命中「懒得笔记」。
- 豁免项逐项独立判定：`V2OApp` 类名 3 处（menu_bar.py:88/:118/:160）＝技术标识、`prog="stage12-status"`（status_cli.py:123）＝技术入口、README.en.md:1/:11 `Video2Obsidian`＝仓库名括注、`.v2o` class（index.html:179）与 `v2o-*` 键＝小写技术标识、`v2o-console-data`（server.py:53）＝禁改项——判定均正确，六文件可见面大写 V2O 非豁免残留 **0 处**。

## 5. 抽检反向证伪（2/2 有牙，/tmp 通道，未用 git checkout）

- 抽检 1：`menu_bar.py:92` 应用名回滚 `V2O Console` → frontend rc=1，断言精确咬住 `super().__init__("V2O Console")`；/tmp 备份还原后 sha256 与原值一致。
- 抽检 2：`status_cli.py:124` help 回滚 `V2O Console` → frontend rc=1，断言精确咬住 `description="Read-only V2O Console status display…"`；还原 sha256 一致，复跑 frontend rc=0。
- 结论：与复核二变异（:47/:124）互不重复的第二、三条独立证据，brand_checks 断言牙真实有效。

## 6. 红线逐项

| 红线 | 证据 | 判定 |
|---|---|---|
| 无 commit/push | HEAD=7bc8cec（=origin/main），工作树改动全未提交 | PASS |
| 无 secrets | 全 diff 六类密钥正则扫描 0 命中 | PASS |
| 未碰封存／008林粒粒AI编程／docs/sop | `git status` 0 命中 | PASS |
| 测试只落 tmp | 本复检脚本/备份/日志全在 /tmp；仓库无新增测试产物 | PASS |
| 主题浅色 | index.html:2 `data-theme="light"`、:364 兜底 `\|\|"light"` 原样 | PASS |
| server.py 零 diff | `git diff --numstat` 无 server.py 行 | PASS |
| No-Clobber | diff 范围＝7 业务/测试文件＋HANDOFF＋两账本＋2 新报告，data/ 等零触碰 | PASS |

净增量重建：numstat 业务/测试 7 文件 34+/16−，与复核二申报精确一致；账本 +6/+1 与 HANDOFF 记载一致。

## 7. DoD 六项判定（supervisor 独立）

① 仓库名不变 PASS（remote=Video2Obsidian 未动）｜② v2o-* 键不变 PASS（探针核验）｜③ 日志前缀不变 PASS（server.py 零 diff）｜④ 数据目录名不变 PASS（server.py:53）｜⑤ 页面/README 改名 PASS（探针 10 落点）｜⑥ 菜单栏/CLI/start.sh 扩展项 PASS（menu_bar 4 改 3 留判定正确＋断言牙实证）。**六项全 PASS。**

## 8. QA-P17-001 裁定

**CLOSED（环境限制补跑完成）。** QA 记录的「codex 沙箱禁 bind 本机端口致 contract HTTP 段 rc=1」属实——但仅限 codex 执行环境；本窗口 supervisor 实跑 contract **482 项 rc=0**（含 HTTP 段），证明该限制非交付物缺陷、亦非「本机不能跑」。原 P2 描述的「需在允许 loopback 的环境补跑」条件已满足，无业务断言失败遗留。P1-3/P1-2 链同类环境问题同口径处理。

## 9. Phase Integrity

HANDOFF diff 逐 hunk 审毕：仅 P1-7 头部/Stage ID/下一步回写＋新增 P1-7 全链节；P0-1/P0-2/P0-3/P1-2/P1-3/P1-4/P1-5 各收口段全部为上下文行一字未碰。两账本为纯追加（+6/+1）。两份 P1-7 新报告未触碰任何旧报告。

## 复检结论

**PASS，无 blocking，P1-7 链 supervisor 累计打回 0/2，链收口。** 非阻断观察 2 条：① 工作树出现 `?? .codebuddy/`（本窗口会话工具产物，非 P1-7 交付物，后续提交勿纳入）；② 复核二禁改清单把主题兜底行号写作 :370（实为 :364），纯笔误不涉判定。下一步按 HANDOFF：提交推送 main（用户已授权），随后默认做 P1-6。

心跳：目标＝P1-7 收口复检｜剩 P0＝0｜下一步＝回传 TM 提交推送。
