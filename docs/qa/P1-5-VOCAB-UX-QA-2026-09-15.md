# BUGS

## 结论

**PASS（代码级 QA；无本任务新增 P0/P1，P3-5 既有缺口仍开放且不阻断）。**

本轮只测不改业务代码；`app/server.py` 未改，sha256 为
`36d78124802d21d07fdb7673b09832943cac5a0a572dbc3f078063c42b70b21f`。
测试只使用外置 `/tmp` 合成数据；未写用户真实视频目录、Obsidian 库或仓库 `data/`。

## 七查证据

| 检查 | 结论 | 命令与 rc |
|---|---|---|
| unit | 过 | `python3 tests/selftest_p1_2_contract.py` → `SELFTEST ALL PASS（482 项断言）`，rc=0。 |
| build | 过（编译级） | `PYTHONPYCACHEPREFIX=/tmp/p15-pycache-20260915 python3 -m compileall -q app src tests`，rc=0。 |
| lint | 未覆盖 | `ruff check app src tests`；本机未安装 ruff，rc=127。`git diff --check` rc=0，仅能证明 diff 无空白错误，不能替代 lint。 |
| API | 过 | 独立外置 tmp 合成数据 + 随机端口 `ThreadingHTTPServer` 真请求 `/api/vocab`、`/api/vocab/candidates`，HTTP 200，端口 61508（`!=8765`），shutdown/close/join 完成；独立探针 rc=0。 |
| logs | 过（本轮运行面） | `find ... -name '*.log' -o -name '*.out' -o -name '*.err'`，rc=0；仅发现仓内既有 benchmark 日志，无本轮服务异常日志。 |
| regression | 过 | `python3 tests/selftest_p1_2_frontend.py` → `FRONT SELFTEST PASS`（157），rc=0；`python3 tests/selftest_v26_presets.py` → `SELFTEST ALL PASS`（58），rc=0。 |
| DoD | 过（P3 已知项除外） | 下表逐条覆盖；`app/server.py` 零 diff，8765 无监听检查 rc=1（预期无监听）。 |

## 12 项逐条结论与现状行号证据

| # | 结论 | 现状证据 |
|---:|---|---|
| 1 限分组标签 | 过 | `app/index.html:1513-1518` 行内标签取 `vocabGroupLabels[group]`，不回显原始 `source`；分组表 `:1477`。 |
| 2 trim | 过（展示层） | `app/index.html:1481-1486` 仅对来源判据 `trim().toLowerCase()`；`data-vocdel`/`esc(e.wrong)` 在 `:1518` 原样透传。后端零改动，`_save` `app/server.py:3004-3016` 原样写盘；`_load` `:2976-2999` 按既有契约读取时 strip、不改大小写；独立 tmp 保存→读取→带空格删除链 rc=0。 |
| 3 空态 | 过 | 空库在 `app/index.html:1502-1508` 只输出一句「词库还没有词条：跑一次 AI 审查生成候选，或手动添加词条。」；有库但过滤零命中仍走组渲染，`:1510-1523` 每组保留「暂无匹配词条」。 |
| 4 筛选计数 | 过 | `:1491-1496` 的 `matched` 与分组数组同一过滤判据；summary `:1500-1501` 输出总数与匹配数。独立 Node DOM 桩过滤 1 条时 summary=总 3/匹配 1，组头仍 4 组，rc=0。 |
| 5 组头点击 | 过（非真机） | `app/index.html:1601-1604` 的组头 label 含 `onclick="event.stopPropagation()"`；仍嵌在 `<summary><label>` 与 `<details>`，三角/空白折叠路径保留。独立真源码 Node 桩验证静态结构；键盘可达性为原生 `summary` 路径，未做真实浏览器像素验证。 |
| 6 全选初态 | 过 | 加载后 `app/index.html:1612-1616` 调 `syncCandidateSelectAll()`；真值计算 `:1623-1625` 为逐条 `every`，默认 low 未勾时全选不勾。提交集合独立核对 `:1888-1889` 只从逐条 checked 映射 indices，与全选框无关；Node DOM 桩验证 high/medium 勾、low 不勾、提交集合 `[0,1]`，全勾后全选及组头均为 true，rc=0。 |
| 7 S11a~S11g | 过 | `tests/selftest_p1_2_frontend.py` 157 项 rc=0；新增断言现状 `:1254-1375`，覆盖分组 trim、标签白名单、空态整条文案、过滤零命中组态、组头 stopPropagation、全选与低置信度初态。独立前端 DOM 桩另 rc=0。 |
| 8 词库三铁律坏例 | 部分通过，既有 P3 | `app/server.py:3037-3039` wrong<2 拒收；独立验证通过。独立验证「正词含 wrong 即删条」被 `_validate_vocab_pair:3019-3053` 接受，未发现该判据；长 wrong 输入不自动排前。 |
| 9 trim 落盘边界 | 过（按现有契约） | 外置 tmp 中 `_save` 原样写入；读取按既有契约 strip；带前后空格的删除请求可命中规范化条目并清空文件。未发现前端 trim 进入 `data-vocdel` 或后端写盘。 |
| 10 全选/提交范围 | 过 | 独立 Node 真源码桩验证页面所见 checked 集合与 `applyVocabCandidates` 的 `ids` 集合同源；不依赖全选框。 |
| 11 筛选/组头同口径 | 过 | `renderVocabList` `:1488-1501` 先过滤再同时计 `matched`、组数组；独立场景总 3、匹配 1、命中组 1，可复算。 |
| 12 空库/过滤零命中 | 过 | 空库提前返回 `:1506-1508`；非空库即使四组全 0 仍执行 `:1510-1523`，独立 DOM 桩确认四个组头和四个组内空态均在。 |

## 独立坏例与重点复验

所有调用 handler 的独立 Python 场景，首个 handler 调用前先断言 `data_root` 的 realpath 位于 `/tmp`；临时目录在 finally 清理。

1. 词库三铁律：独立调用真实 `_validate_vocab_pair` 验证单字 wrong 被拒；构造「正词含 wrong」被接受；输入短 wrong 在前、长 wrong 在后，读取顺序不变；写入 602 条后读取侧只暴露 500 条，但 `_save_vocab_entries` 私有层物理落盘 602 条，说明“上限 500”由 API/读取侧部分保证，不是保存层统一不变量。
2. trim 保存→读取→删除：`wrong/right/source` 含前后空格时磁盘 JSON 保留原样；读取按现有约定 strip；删除请求携带前后空格可命中并删除。前端 `data-vocdel` 仍使用 `esc(e.wrong)`。
3. 组头点击：独立从真 `app/index.html` 读源码，确认 stopPropagation、`details/summary` 嵌套；Node DOM 桩确认点击组头逻辑不改折叠结构。证据等级为“真源码静态 + DOM 桩”，不是浏览器像素/真实点击证据。
4. 全选初态与提交范围：合成 high/medium/low，low 默认不勾；`syncCandidateSelectAll` 与组头状态正确；逐条全勾后全选和三组组头均勾；提交 ids 取逐条 checked。
5. 筛选计数：合成 3 条词库，过滤命中 1 条；summary 为「已导入词库（3条）· 匹配 1 条」，组头保留且命中组为 1。
6. 空态与过滤零命中：空库只一句人话；非空库过滤零命中仍四组显示、每组「暂无匹配词条」，两种情景未混淆。
7. 真 HTTP：`ThreadingHTTPServer(('127.0.0.1', 0), Handler)` 实跑 `/api/vocab`、`/api/vocab/candidates`，外置 data_root 回传正确，随机端口 61508，shutdown 后线程退出，rc=0。
8. 主动证伪：检查 `git diff --quiet -- app/server.py` rc=0、哈希与给定基线一致；静态复验浅色主题、D-12 `_strip_paths`、No-Clobber、FR-13/14/17、P1-3 五桶/elapsed/运行锁/二选一，均 PASS；未发现本批回退。

## BUG 清单

| Bug ID | Priority | Stage P0 Blocking? | Repro | Status | Current Task | 备注 |
|---|---|---:|---|---|---|---|
| P1-5-P3-5-001 | P3 | 否 | 外置 tmp 调 `_validate_vocab_pair('独立错词','正确的独立错词文本',set(),set())` 返回 `None`；短 wrong 在长 wrong 前保存后顺序不变。 | OPEN（既有） | DEVELOP-P1-5 | 复核确认：代码/数据无“正词含 wrong 即删条”与“长 wrong 排前”的机械保证；非本批引入，不升 P0。 |
| P1-5-P3-5-002 | P3 | 否 | 外置 tmp 直接调用 `_save_vocab_entries` 写入 602 条，物理 JSON 为 602；读取侧切到 500，API add/apply 另有容量判断。 | OPEN（既有） | DEVELOP-P1-5 | 上限 500 不是保存层统一不变量；本批后端零改动，按前置评审 P3-5 记账。 |

## DoD 覆盖表

| DoD | 结论 | 证据 |
|---|---|---|
| 六项词库展示易用性：标签、trim、空态、筛选计数、组头点击、全选初态 | 过 | `app/index.html:1477-1523`、`:1601-1625`；前端 157/157 rc=0；独立 Node DOM 桩 rc=0。 |
| V1.3 CANDIDATE-UI2 P3-3/P3-4 | 过 | `:1601-1625`；组头 stopPropagation、details/summary 折叠路径、真实 checked 同步、逐条提交集合。 |
| V1.3 VOCAB-FOLD P3-1~P3-4 | 过 | `:1481-1523`；四组固定标签、展示判据 trim、单句空态、过滤总数/匹配数同口径。 |
| 词库落盘不被展示 trim 改写 | 过（现有读边界） | `app/server.py:2976-3016`；独立 tmp 原样落盘、读取既有 strip、删除链通过；server 零改动。 |
| 前序成果无回退 | 过 | `app/index.html:2` 浅色；`app/server.py` `_strip_paths`；No-Clobber/FR-13/14/17/P1-3 静态复验与三套自测 rc=0。 |
| 三铁律完整机械保证 | 不完全，既有 P3 | wrong≥2 有校验；“正词含 wrong 即删”“长 wrong 排前”未机械保证；上限 500 由 API/读取侧部分保证，见 BUG P3-5-001/002。 |

## 前序成果无回退

- 默认浅色：`app/index.html:2` 保持 `data-theme="light"`。
- D-12：`app/server.py` 保留 `_strip_paths`；本链 `server.py` 零 diff。
- No-Clobber 只判不写；P0 FR-13/14/17、P1-3 五桶同源 fail-closed、elapsed、运行中锁定、二选一不双空均由回归/静态复验覆盖。
- `app/server.py` sha256 与基线一致；未启动 8765，未触碰真实数据。

## 未覆盖项

- 真机读屏、截图、点击、输入、滚动、像素 UI 目检：**NOT_VERIFIED**。
- 未使用真实浏览器/Headless Chrome；组头点击证据为真源码静态 + Node DOM 桩，键盘路径依赖原生 `summary`，未做像素级验证。
- lint：ruff 未安装，rc=127；无其它项目 lint 配置可执行。
- 真实 whisper、真实视频/Obsidian 旅程、长视频耗时、多标签真实并发：未覆盖。
- 三铁律 P3-5 的既有缺口未修复（本任务硬约束只测不改）。

## 真机 QA 会话能力预检结果

- 日期/任务名：2026-09-15 / `DEVELOP-P1-5`「词库与候选易用性修整」
- session ID：N/A（代码级 QA，未开启真机 session）
- 模型精确 ID：`codex/gpt-5.6-luna`（按 `USER_MODEL_OVERRIDE.md`）
- Runtime：未启动真机 Runtime
- 原生 CUA 是否实际注入：未注入、未验证
- 可用工具精确名称：本轮实际使用 shell、Python、Node；无读屏/截图/点击工具
- CLI 备用入口是否存在：未验证
- Orca Runtime 状态／能力／权限：未查询，故不编造
- 读屏／截图／点击并恢复／输入并清除／滚动／界面恢复：均未执行
- 最终结论：**NOT_VERIFIED**
- 是否允许进入正式 QA：否；仅代码级 QA 结论有效，**不得写“真机 QA 已启用”**。

## Fix Attempt Fingerprint

- Task ID：`DEVELOP-P1-5`
- Root Cause Hypothesis：本批为前端展示层修整，主要风险是展示归一化误入落盘/提交边界，以及组头、全选、筛选统计口径不一致；另需确认既有 P3-5 三铁律是否真的有机械保证。
- Approach：三套仓内自测；现状行号核对；外置 `/tmp` 合成词库/候选；真实 handler；随机端口 HTTP；独立 Node 真源码 DOM 桩；hash/diff/regression 静态复验。
- Files Changed：本次只新增本报告；未改业务代码、测试代码或其他 docs；未 commit/push。工作树中其它改动为任务前已存在，未触碰。
- Verification：contract 482/rc0；frontend 157/rc0；presets 58/rc0；compileall rc0；diff-check rc0；独立 Python/HTTP/Node 探针 rc0；server 零 diff/hash 一致；8765 无监听。
- Failure Reason：无 P0/P1 产品失败；P3-5-001/002 为前置评审已知、独立复验仍成立的非阻塞缺口；lint 因 ruff 未安装未覆盖。
- Difference From Previous Attempt：本任务首次 QA；新增独立保存→读取→删除 trim 链、602 条容量边界、正词包含 wrong、长 wrong 顺序、过滤零命中、真 HTTP 和全选/提交集合一致性复验。

心跳：目标＝完成 P1-5 代码级 QA｜剩 P0＝0｜下一步＝提交报告给 supervisor 复检。

---

# supervisor 复检（2026-09-15）｜DEVELOP-P1-5「词库与候选易用性修整」全链收口

- 复检人：supervisor（`opencode-go/muse-spark-1.3-contributor`，本窗口，按 `USER_MODEL_OVERRIDE.md` 行；角色卡 `docs/roles/supervisor.md`）。
- 范围：**不读报告下结论**——本节每条都是我自己跑出来的（实跑 rc、自算 sha256、自写探针、自己造坏例、真 Chrome 真实 DOM）。**未改业务代码、未改任何报告正文、未 commit/push**；本文件只在尾部追加本节。
- 结论：**PASS｜无 blocking｜本链 supervisor 累计打回 0/2**（链内 0 次 FAIL）。放行收口。
- 基线（动手前我自己复算，与派单书逐字一致）：`app/index.html`=`b412ba99fae801a15d3225755f9da73e469625b3483ba19fa83ce29d02cb92ed`、`tests/selftest_p1_2_frontend.py`=`49e0b336…9160cb4`、`tests/selftest_p1_2_contract.py`=`03719d45…a6227fa`、`app/server.py`=`36d78124802d21d07fdb7673b09832943cac5a0a572dbc3f078063c42b70b21f`；`HEAD`=`e340396`（＝`origin/main`，P1-5 交付**全在工作树、未提交**）。`git status --porcelain`＝6 `M`＋2 `??`，与 HANDOFF 口径一致；`docs/sop/` 目录不存在（零接触）；`src/` 零改动。

## 一、实跑三套自测（看 rc，不看打印）

| # | 命令 | 实测结论行 | 断言数（`grep -c '^PASS'`） | rc |
|---|---|---|---|---|
| 1 | `python3 tests/selftest_p1_2_contract.py` | `SELFTEST ALL PASS（482 项断言）` | 482 | **0** |
| 2 | `python3 tests/selftest_p1_2_frontend.py` | `FRONT ALL PASS` ＋ `FRONT SELFTEST PASS` | **157** | **0** |
| 3 | `python3 tests/selftest_v26_presets.py` | `SELFTEST ALL PASS` | 58 | **0** |

三套数字与派单书（482／157／58）**逐个吻合**。另实测：`git diff --check` rc=**0**；`git diff --numstat`＝`app/index.html 29/5`、`tests/selftest_p1_2_contract.py 48/0`、`tests/selftest_p1_2_frontend.py 182/2`（＋HANDOFF 21/12、两账本 5/0＋1/0），与派单书逐字一致；**8765 无监听**（`lsof -nP -iTCP:8765 -sTCP:LISTEN` 无输出）。

## 二、两道账本校验（exit 码为准）＋坏行负控

| 账本 | 行数 | 校验命令 | 实测 | 结果 |
|---|---|---|---|---|
| `docs/model/TASK-MODEL-LOG.jsonl` | **52** | 角色卡整块照粘（11 键＋`result`/`escalated` 枚举＋`rework` 非 int 含 bool 判负） | 无坏行输出 | **exit 0** |
| `docs/model/DISPATCH-LOG.jsonl` | **68** | 角色卡整块照粘（8 键＋`used` 恒`主`＋`result`/`runtime` 枚举） | 无坏行输出 | **exit 0** |

**坏行负控（证明校验器真有牙、不是空转）**：把整块校验器落到 `/tmp/sup_p15/chk_task.py`／`chk_disp.py`，自造坏例跑——

- TASK 侧 5 类坏例全被抓：JSON 坏／`result=MAYBE` 枚举错／缺键 `tokens`／`rework=True`（bool 混入 int）／`escalated=TRUE` 枚举错 → **`exit 1`**。
- DISPATCH 侧 4 类坏例全被抓：`used=备` 非常量主／`result=OK` 枚举错／`runtime=openai` 枚举错／JSON 坏 → **`exit 1`**。

→ **9 类坏例 100% 被咬住，两校验器均 `exit 1`**；好文件 `exit 0` 且静默。校验器**有牙**。

## 三、三处对账（本链 5 派 ↔ 两账本）

| 派次 | HANDOFF（§一 P1-5 执行链） | `DISPATCH-LOG` | `TASK-MODEL-LOG` |
|---|---|---|---|
| ① | builder 交付 **PASS** | **L64** builder／`opencode-go/deepseek-v4.1-flash`／本窗口／主／PASS | **L52** role=builder／model=`opencode-go/deepseek-v4.1-flash`／PASS／`rework=0`／`escalated=NO` |
| ② | code-reviewer 首轮 **PASS** | **L65** code-reviewer／`opencode/muse-spark-1.3-contributor-free`／本窗口／主／PASS | （本链任务级 1 行，同上） |
| ③ | builder 返工 P3-3／P3-4 **PASS** | **L66** builder／`opencode-go/deepseek-v4.1-flash`／本窗口／主／PASS | 同上 |
| ④ | code-reviewer 复核二 **PASS** | **L67** code-reviewer／`opencode/muse-spark-1.3-contributor-free`／本窗口／主／PASS | 同上 |
| ⑤ | qa 独立 QA **PASS** | **L68** qa／`codex/gpt-5.6-luna`／codex／主／PASS | 同上 |

- **role／model／runtime／result 逐字核根表（`USER_MODEL_OVERRIDE.md` 4 列版）**：5 行**全部逐字一致**。
- **本链不匹配行数 ＝ 0**（5 派 ↔ 5 行 ↔ 1 行，行数与字段全对上）。`DISPATCH-LOG` 68 行＝P1-4 收口时的 63 行 ＋ 本链 5 行；`TASK-MODEL-LOG` 52 行＝51＋1，**加法自洽**。
- **全文件扩核（非本链）**：`DISPATCH` **L1／L8** 两行与现行表不符（`supervisor=deepseek-v4.1-flash/codebuddy`、`builder=codex/gpt-5.6-luna/codex`）＝**2026-09-14 表换代前的历史行**，已由 P1-4 复检节交代，**属历史快照、非本链缺口、不返工**。`TASK-MODEL-LOG` 首 41 行同样与现行表不符——**这是账本的设计语义**（一行一任务、记**当时实际用的模型**，正是「换模型决策的依据」），**不是失实**；本链只用 L52 一行、已对齐。
- **「本链 supervisor 复检行尚未记」裁定**：**不算缺口**。本链 `supervisor 复检` 行按体例由 TM 在我出结论后追加（P1-3／P1-4 同款：复检当场未记、由 TM 落盘后补），且 `DISPATCH-LOG` 迄今每一行的 `result` 都已由我核过；本行缺失**不影响三处对账（本链业务 5 派已 0 不匹配）**，也**不影响收口**。

## 四、12 项逐条到源码核（Plan 六项 ＋ 处置表 6 条）

| # | 项 | 判定 | 我实测的源码证据（工作树行号） |
|---:|---|---|---|
| 1 | 限分组标签（Plan①） | **在位** | `app/index.html:1518` 行内标签取 `vocabGroupLabels[group]`；`data-vocdel="'+esc(e.wrong)+'"` 与 `esc(e.wrong/right)` **一字未动** |
| 2 | `VOCAB-FOLD P3-1` | **在位** | 同上；我的真函数渲染实测 `candidate`／`whatever` 在整块 markup **0 命中**，标签 ⊆ 四组标签 |
| 3 | trim（Plan②） | **在位** | `:1482` `String(...).trim().toLowerCase()`，**只此一处**（全仓 `grep -n '\.trim()'` 逐行核过：其余 30 处均非本链新增） |
| 4 | `VOCAB-FOLD P3-2` | **在位** | 同 `:1482`；探针实测 `  programming  `→预置-编程、` preset:CRYPTO `→预置-币圈 |
| 5 | 空态（Plan③） | **在位** | `:1506-1509` 早返回；判据是 `!loadedVocabEntries.length`（**整库**）**不是**命中数 |
| 6 | `VOCAB-FOLD P3-3` | **在位** | `:1507` 文案＝「词库还没有词条：跑一次 AI 审查生成候选，或手动添加词条。」 |
| 7 | 筛选计数（Plan④） | **在位** | `:1491` `matched++` 在过滤 `return` **之后**；`:1500-1501` summary 追加 `· 匹配 N 条`（仅 `filter` 非空） |
| 8 | `VOCAB-FOLD P3-4` | **在位** | 同 `:1500-1501`；组头 `:1513` 用过滤后 `items.length` → 与 `matched` **同口径** |
| 9 | 组头点击（Plan⑤） | **在位** | `:1604` `<label class="candGroupHead" onclick="event.stopPropagation()">` |
| 10 | `CANDIDATE-UI2 P3-3` | **在位** | 同 `:1604`；`<details class="candGroup">`／`<summary>` 两层未动，折叠三路保留（见§六-C） |
| 11 | 全选初态（Plan⑥） | **在位** | `:1615` 由 `all.checked=false` 改为 `syncCandidateSelectAll()`（本体 `:1623-1625` 未改） |
| 12 | `CANDIDATE-UI2 P3-4` | **在位** | 同 `:1615`；口径＝`cs.length>0 && cs.every(checked)`（`:1624`） |

- **空态与「过滤零命中仍按组显示」互斥性**：真函数实测——真空库 → 单句、0 组块、0 句「暂无匹配词条」；非空库过滤零命中 → **4 个组块 ＋ 4 句「暂无匹配词条」**、Σ组头＝0。两者**未混淆**。
- **组头过滤数与 summary 匹配数互相复算**：未过滤 N=3、Σ组头=3；过滤命中 → `（3条）· 匹配 1 条`、Σ组头=1；零命中 → `匹配 0 条`、Σ组头=0。**三档恒等式 `Σ组头 ≡ matched` 成立**。
- **行号核对（如实记）**：首轮报告 §二 的 `:1503-1506`／`:1515`／`:1612`／`:1620-1622` 系**返工前快照口径**（返工在 `renderVocabList` 顶部加了 3 行理由注释，其后行号整体 +3）；复核二正文多数已改用返工后行号（`:1507`／`:1625` 均**准确**），仅标题/§三-D 引用的 `:1612` 未同步（应为 `:1615`），`:1620-1622` 应为 `:1623-1625`。**判：文档级行号局部漂移、非失实、不阻断**（与 P1-3 先例同款），留收尾 neat 加注。QA 报告 `:1612-1616`（区间已覆盖 `:1615`）与 `:1623-1625` **准确**。

## 五、词库三铁律 —— 我的独立裁定（级别 ＋ 是否阻塞）

先给独立证据（全部我自己跑，不复述报告）：

| 铁律 | 有无机械保证 | 我的独立证据 |
|---|---|---|
| ① wrong ≥ 2 字 | **有** | `_validate_vocab_pair`（`server.py:3019-3053`）与公开入口（`_handle_vocab_add:3895`、预置 `_read_vocab_preset`）双重在位；我实测 `('a','bbbb')` 被拒、`('，。','bbbb')` 被拒。预置 3 域 **369 条实测 0 条违规** |
| ② 正确文本含 wrong 即删条 | **无**（全仓无此判据） | 实测 `_validate_vocab_pair('独立错词','正确的独立错词文本',set(),set())` → **`None`（放行）**；且该对子被 `_register_user_rules` **真的注册进替换表**，把「这是一段独立错词的测试」改成「这是一段**正确的独立错词文本**的测试」（自指扩展，可观测） |
| ③ 长 wrong 排前 | **无** | `src/stage3/normalize.py:132-166` `apply_corrections` 按**表序**逐条 `if pattern in text: replace`；表序＝`_register_user_rules:3087` 的 `base + pairs`（**追加序**）。实测用户段顺序 `["local","env local"]`（**短在前**）→「在 env local 里」被改坏成「在 env **本地** 里」（长对子命中不了）；对照把长的放前 → 「在 .env.local 里」正确 |
| ④ 上限 500 | **部分**（公开侧有、私有落盘层无） | `VOCAB_MAX_ENTRIES=500`（`:2958`）＋读取截断（`:2999`）＋公开入口容量门（add `:3903`、预置 `:3462`、`:4147`）**都在位**；但 `_save_vocab_entries`（`:3004-3016`）**不检查上限** → 我实测直接调它写 602 条，`vocab-user.json` **物理 602 条**；读取侧只暴露 500；公开 `add` 在满 500 时确实 400「词库已满」 |

**裁定（级别）**：**P3**。

- **不升 P0**：Plan `docs/pm/PRODUCT_PLAN.md:222` 明写本项「不做不阻断失败恢复，**不得升 P0**」，全链据此把握级别。
- **为什么不升 P2**：①②③④ 四条**全部是既有缺口**——本链 `app/server.py` 与 `src/` **零改动**（`server.py` sha256 与基线逐字一致、`git status --porcelain -- src/` 为 0 行），`_save`／`_load`／`_validate`／`apply_corrections`／`_register_user_rules` 均**不可能由本批引入**；且实测影响面**有界**：真实预置 369 条**自指 0、短错词 0**，基表 5 条**自指 0**，公开 HTTP 面**无法**落盘超 500 条。缺口只在「用户自己造出自指对子」或「内部代码绕过公开入口直调私有层」时可见，**不涉** No-Clobber／任务身份／D-12 脱敏／失败恢复任何一条主线不变量。
- **是否阻塞收口**：**不阻塞**。`_load` 对超限做截断（`:2999`）使超 500 无下游后果；②③ 是「用户自造」而非「工具自己变坏」。两条已如实挂在 `QA 报告 BUG 表 P1-5-P3-5-001／002` 与 `CODE-REVIEW §五 P3-5`，**OPEN ＋ 属既有 ＋ 标注级别**，未含糊。
- **附：我独立发现的技术约束（对 TM 择方案有实质影响，两份报告都未提）**：`_user_rules_revision`（`:3056-3067`）对 pairs 做 **`sorted()`**，而替换表 `base + pairs` **不排序** —— 两者口径不一致。我实测：先把 `[("env local",".env.local"),("local","本地")]` 注册后，再以**换序**的同组对子注册，`_register_user_rules` 直接抛 `ValueError: 内存词表 's9-corr-v2-user-a4572e07' 内容冲突，拒绝替换`（`:3091` 的碰撞守卫）。**含义**：任何「长 wrong 排前」的修法必须**同时**改 revision 规范化与表构造，否则会把「同内容异序」打进 `内容冲突→拒收` 的死路（例如 `.env.local` 类对子）。另：基表恒**前置**于用户对子，故用户对子的 wrong 若是某条基表的子串，用户规则**永不生效**——这也是「排前」缺口的同源后果。
- **交给用户的选项（HANDOFF §二-3 已列，我补一条判据）**：① 只补口径文档（**零行为风险，我倾向此项**——Plan 未要求、四条铁律中三条半已在代码/数据侧成立）；② 补代码保证 = **行为变更**（改 `apply_corrections`／`_register_user_rules` 的排序与删条，Change B 级，**须同时改 revision 规范化**，并过全链 code-reviewer＋qa＋supervisor）；③ 补断言把现状钉死（改测试，不碰行为）。

## 六、自写独立探针（不复用仓内自测）

一律只落系统 tmp／合成数据；凡调 handler 的用例首行 `assert_tmp`（我自己的版本）。

**A. 后端探针（`/tmp/sup_p15/probe_backend.py`，自己 import 真 `app/server.py`）——21/21 PASS，rc=0**

| 覆盖 | 关键结果 |
|---|---|
| ① 带前后空格词条 保存→读取→删除 全链 | `_save` 逐字落盘（磁盘 JSON ＝`{"wrong":"  空格错词  ",...}` 含空格原样）；`_load` 既有 strip；`_user_rules_revision` 对空格**敏感**（带空格串 ≠ 去空格串）⇒ 落盘串未被展示层归一化污染；真 `_handle_vocab_del` 用**前端原样串（带空格）**删 → **200 并清空**（原始文本仍是删除判据）；真 `_handle_vocab_add` 落盘＝**handler 自己的 strip 结果**（既有后端归一化，**非**本链前端 trim） |
| ② 四铁律 | 见§五（①有②③无④部分） |
| ③ 落盘链路字面 | `_save` 不含 strip；`_load` 只 strip **不改大小写**（段内 `.lower()` 0 命中）；`_validate` 只对**自己副本** strip，不改写落盘内容 |

> 我探针首轮曾因**漏传 `data_root`** 命中 handler 的 `DEFAULT_DATA_ROOT` 回落（`server.py:53`＝`tempfile.gettempdir()/v2o-console-data`，**系统 tmp 内、非仓内 `data/`、非用户真实目录**），已补参重跑并**清理该残留目录**（复查 `test -e` → NO）。如实记：这是**既有**回落行为（`normalize_path(params.get("data_root")) or DEFAULT_DATA_ROOT`），本链未改、未放开，且不落真实目录。

**B. 前端探针（`/tmp/sup_p15/probe_front.js`，自写配平抽取真源码 ＋ 自建 DOM 桩）——36/36 PASS，rc=0**

| 覆盖 | 关键结果 |
|---|---|
| ① 过滤计数复算 | 未过滤 `已导入词库（3条）`／Σ组头=3；命中→`（3条）· 匹配 1 条`／Σ组头=1；零命中→`匹配 0 条`／Σ组头=0 且**4 组块仍在**、4 句「暂无匹配词条」；恒等式 `Σ组头 ≡ matched` **三档成立** |
| ② 空态 | 真空库→单句、0 组块、0 按钮；文案 **[上下左右] 0 命中**（无方位词）；含「手动添加」 |
| ③ 带空格链 | `data-vocdel=" 空格错词 "`（**含前后空格原样**）；`  programming  `→预置-编程；行内标签四组之内、`candidate`／`whatever` 0 命中 |
| ④ 全选初态（含 low） | 夹具 high/medium/low 各 1 条，low 默认不勾 → 全选框 `false` **且 == 逐条 `every` 复算**；组头 high=true/medium=true/low=false；**提交集合 == `[0,1]` == 页面所见勾选**（全选框**不参与**提交）＝**页面所见＝提交范围**；取消一条已勾项 → 组头与全选**跟随**；全勾 → 全选 true ＋ 三组头全 true、提交集合 `[0,1,2]`；组头勾选 → **只切本组、别组不动**；空组（0 条）→ 组头**真值 false**（markup 默认 `checked`，**与新断言相反 ⇒ 断言有判别力**） |
| ⑤ 组头点击／折叠结构 | 真 markup：**3/3** 组头带 `onclick="event.stopPropagation()"`；`<details class="candGroup" open><summary><label class="candGroupHead"…><input…></label></summary>` 三层结构完整；`open` 只给 high/medium（low 默认收起 ⇒ 折叠路径仍在） |

**C. headless Chrome 真实 DOM 事件实验（证据等级：真浏览器行为级）**——用**真函数渲染出的真 markup**（`/tmp/sup_p15/markup.txt`）落 `/tmp/sup_p15/dom.html`，`Google Chrome --headless=new --dump-dom` 实测：

```
组头数=3|组数=3|labelW=128|summaryW=738|summaryFocusable=true
N1 点勾选框：勾选翻转=true | details.open 未变=true     ← 勾选只勾选、不折叠
N2 点 label 文字：details.open 未变=true
N3 点 summary 本体：折叠生效=true                      ← 折叠路径仍可达
N4 summary 拿到焦点=true | Space 路径折叠生效=true      ← 键盘路径可达
N5 对照组（无 stopPropagation）点勾选框：details.open=true  ← 本机 Chrome 改前也不折叠
```

- **裁定**：三条并存——① 勾选只勾选（`stopPropagation` 在位且**不** preventDefault，勾选态照常翻转）；② 折叠**三路**（summary 本体／label 外空白 **738−128＝610px**／键盘 Space）**全部可达**，「整行只剩小三角能点」的退化**不成立**；③ **独立确认** code-reviewer §三-C 的事实澄清：**本机 Chrome 下改前形态点勾选框本来也不折叠**，故这条改动在本浏览器属**防御性保留**（对会冒泡折叠的浏览器有效），**无副作用、也无本项目可观测收益**——真机需实点一次，记 P3（并入真机目检），**不阻断**。

## 七、抽检反向证伪（自己动手）＋全量 sha256 还原

动手前我自己备份到 `/tmp/sup_p15/bak/`（**全程未用 `git checkout`**），逐条「改坏→跑→要求 rc=1→从备份拷回→比对**全量** sha256」。

| 变异 | 内容（我自选，不照抄报告） | rc | 咬住它的断言 |
|---|---|---|---|
| **M1** | 删掉 `:1615` 的 `syncCandidateSelectAll();` 调用 | **1** | `S11g 全选初态＝真状态` ＋ `S11g 空组组头按 0 条算` |
| **M2** | 删掉 `:1625` 的**组头回写**语句 | **1** | `S11g 取消已勾项后组头与全选跟着取消（组头回写被删即 rc=1）` ＋ `S11g 空组组头按 0 条算` ——**与复核二自陈的失败项名/值逐字一致** |
| **M3** | `matched` 改成计**全量**（匹配数恒等总数） | **1** | `S11d 匹配数随过滤缩小（总数不变）` |
| **M4** | 关掉空库早返回（`if(!loadedVocabEntries.length)`→`if(false)`） | **1** | `S11e 空库只留一句人话` ＋ `S11e 空库空态文案整条一致` |
| **M5** | 空态文案回退「或在**上方**手动添加」 | **1** | `S11e 空库空态文案整条一致（不含方位词…）` |
| **M6** | `server.py` `_save_vocab_entries` 改成 `str(...).strip()` 落盘 | **1** | 契约 `13a 保存逐字落盘（不 trim、不动 source 原样）` |
| ~~M7~~ | 我自造的「把 `matched++` 挪到 `groups.push` 之**前**」 | 0 | **不是变异**（两条语句独立，语义等价）——**我自己判废、不计入覆盖洞**（自纠 1 处） |

**还原校验**：`shasum -a 256` 4/4 文件与动手前基线 **逐字一致**（`diff /tmp/sup_p15/baseline.sha256 /tmp/sup_p15/after.sha256` 无差异）；`git status --porcelain` 仍是原 6 `M` ＋ 2 `??`；`git diff --numstat` 数值未变。**自测确实有牙（6/6 真变异全 rc=1）**。

## 八、未修项是否被如实申报（逐条比对）

- **首轮 7×P3**：`P3-1`（组头 stopPropagation 防御性）／`P3-2`（空库仍带「匹配 0 条」）／`P3-3`（文案方位词 — **已闭**）／`P3-4`（断言假信心 — **已闭**）／`P3-5`（三铁律既有）／`P3-6`（行内标签抹平、不返工）／`P3-7`（空/未加载/失败不区分，既有）——**7/7 逐条在 `CODE-REVIEW §五`，带编号与归属**。
- **复核二新记 3×P3**：`P3-新1`（关持久影子时 TypeError 中断）／`P3-新2`（`el("vocabList")` 桩 `querySelectorAll` 恒空，删除点击链不可达）／`P3-新3`（`:1338` 断言可挪位）——**3/3 在 `复核二·七` 末**。
- **QA 2×P3**：`P1-5-P3-5-001`／`P1-5-P3-5-002`——**在 QA 报告 BUG 表，标 OPEN（既有）**。
- **结论：12 条 P3 全部单独编号、可见、未含糊**；已闭 2 条标注「已闭」并给出证伪证据，未闭的标注「仍开（既有／本批）」。**无一条被含糊掉**。
- **我另外核出 2 处文档级小瑕疵（均不阻断，交 neat）**：① 首轮报告说「base 表只有 3 条」，实际 `_register_user_rules` 用的 `table = base + pairs` 里 base＝`full_table()`＝**5 条**（3 frozen＋`Github`／`Vscode`）——**不影响结论**（两条路径实测都不是长度降序）；② §四的局部行号漂移（见§四末）。

## 九、Phase Integrity 五查

| # | 查项 | 实测 | 结论 |
|---|---|---|---|
| 1 | PLAN 阶段禁 builder／qa 业务派工 | `PROJECT_PHASE=DEVELOP`（HANDOFF:7 ＋ 用户 2026-09-14 明确进 Phase2）；本链派工＝builder／code-reviewer／qa，**合规** | **过** |
| 2 | WAITING_HUMAN_APPROVAL 禁自动开发 | 状态非 WAITING；HANDOFF `PLAN_GATE=APPROVED`（用户明确进 DEVELOP） | **过** |
| 3 | DEVELOP 必有 DEV_BASELINE | HANDOFF:11 `DEV_BASELINE=PRODUCT_PLAN_V1.3`，与 Plan 正文 V1.3 一致 | **过** |
| 4 | C 类变更禁绕 Controlled Reopen | 本链 `CHANGE_REQUEST=B`（Plan:222 直接定义 P1-5 五项＋处置表 6 条，交付**未引入**产品/架构变更）；无 `PLAN_REOPEN_REQUIRED` 需求 | **过** |
| 5 | TM 停摆沿用 watchdog | 本链无停摆；三处对账 0 不匹配、账本已落盘 | **过** |

## 十、红线抽查

| 项 | 实测 | 结论 |
|---|---|---|
| 无 commit／push | `HEAD=e340396` ＝ `origin/main`；`git log -1` 仍是 P1-4 那条；交付全在工作树 | **过** |
| 无 secrets | `git diff \| grep -ciE 'api[_-]?key\|secret\|token *=\|password\|sk-[A-Za-z0-9]{16,}\|BEGIN …PRIVATE KEY'` → **0** | **过** |
| 未改封存件 | `git status --porcelain \| grep -iE 'V1\.10\|V2\.0\|封存'` → **0** 行 | **过** |
| `docs/sop/` 仅模板示例 | 该目录**不存在**（`git status -- docs/sop/` 报 no such directory）→ 零接触 | **过** |
| 测试只写外置 tmp＋合成数据 | 新测试代码 `git diff -- tests/` 内 `/Users/`、`/Volumes/`、`8765`、`Documents`、`Obsidian`、`Downloads`、`realpath(` **0 命中**；契约新块首行时序正确（`:2047 make_data_root` → `:2048 assert_tmp`，**紧跟**）；前端 S11 走 `FETCH_QUEUE` 假响应（`data_root:"/tmp/p12-s11"`），**不发真请求** | **过** |
| **没写到用户真实词库或 `data/` 下真实文件** | 仓根 `data/` **不存在**；`DEFAULT_DATA_ROOT`＝系统 tmp（`server.py:53`）；我全程只落 `/tmp/sup_p15` 与系统 tmp，探针残留已清 | **过** |
| 主题默认浅色 | `app/index.html:2` ＝ `<html lang="zh-CN" data-theme="light">`；本链 diff 内 `data-theme` **0 命中** | **过** |
| **D-12 口径（`_strip_paths`）未被放宽** | `_strip_paths` 仍在 `server.py:354`；**因 `server.py` 全文件 sha256 与基线逐字一致 → 该函数字节不可能变**（复核二/QA 记录 828 字节，我独立复核段内无 `os.path.isabs` 放行分支、`/Users` 仍在抹除清单） | **过（未放宽）** |
| No-Clobber | 全部在 `server.py`，本链**零改动** → 不可被触碰；契约 `7g`（目标已存在→SKIPPED 未覆盖／库内字节未变／缺库不重建）**482/482 全 PASS** | **过** |
| `server.py` 真零改动 | `git diff --numstat -- app/server.py` **0 行**；`shasum -a 256` ＝ `36d78124…b21f`，与派单书期望值**逐字一致**（我独立复算） | **过** |
| 测试污染 | 未起 8765、未请求线上、未写 `state.db`；本链为纯前端展示层修整，不涉 vault | **过** |

## 十一、前序已收口语义未回退

- **P0（FR-13／FR-14／FR-17）**：`app/server.py` **零改动（sha256 逐字一致）** ⇒ 后端侧**逐字未动**；前端 `#recoverBox`／`say` 截断／可关闭长面板 在本链 `index.html` diff 内 **0 命中**（`git diff -- app/index.html \| grep -E 'recoverBox|data-theme|digestEntry|FAIL_DIGEST_FIELDS|vocabApplyPct|STATE_BUCKET|batchLockHint|diagCache'` → **空**）；前端 S8d／S9a／S9b（运行中锁定、可离开文案、共用锁定提示）**157/157 全绿**。
- **P1-3（无目标不画 100%／运行中锁定／五桶同源 fail-closed／elapsed／二选一不双空）**：后端 `STATE_BUCKET`／`_state_bucket` 在 `server.py`（零改动）；前端 `vocabApplyPct`／`setCandidateControls`／`batchLockHint` 不在本链 diff；契约 `10a／10a2／10a3／10a4／10b／10e／10f` 与前端 `S8a~S8j2` **全 PASS**。
- **P1-4（脱敏摘要白名单 `FAIL_DIGEST_FIELDS` ＋ 唯一出口 `digestEntry`）**：`grep -c` 前序关键字在 `server.py` 命中 **23** 处、在 `index.html` 命中 **47** 处，均在位；契约 `11b／11g`（白名单恰 7 类、无第 8 类出口、无全路径导出入口）与前端 `10d`（白名单外键无出口）**全 PASS**；本链 diff 内这些标识符 **0 命中**。
- **结论**：**三链已收口语义一行未碰**（后端由 sha256 全文件相等**证明**，前端由 diff 关键字 0 命中 ＋ 157/157 回归**证明**）。

## 十二、遗留 backlog（非阻塞，供 TM 排期）

**本链内（我复检新增/确认）**
1. 三铁律 ②③ 无机械保证（**P3，既有**）＋ 私有落盘层可超 500（**P3，既有**）→ `P1-5-P3-5-001／002`，OPEN。**等用户从 HANDOFF §二-3 三选项里拍一句**（我倾向①，零行为风险）。
2. 首轮 `P3-1`：组头 `stopPropagation` 在本机 Chrome 属防御性保留（我独立复现），**真机实点一次**确认（并入真机目检）。
3. 首轮 `P3-2`：空库时 summary 仍带 `· 匹配 0 条`（一行条件可省）。
4. 首轮 `P3-6`：行内标签抹平后「候选导入 vs 我手加」不可区分（**Plan 字面支持，不返工**；若要区分加来源副标）。
5. 首轮 `P3-7`：`loadVocab:1533` 失败静默 → 5xx 时可能被告知「词库还没有词条」（**既有**）。
6. 复核二 `P3-新1／新2／新3`（测试体例与桩覆盖；`P3-新2` 的**词库删除按钮点击链**建议并入真机目检）。
7. **文档级**：首轮 §二 行号系返工前快照（+3 漂移）；「base 表 3 条」应为 5 条 → 留 neat 加注（**不改报告正文**）。
8. **真机 UI 未目检**：本链 3 次相关判断（code-reviewer／QA／我）**均标 NOT_VERIFIED**，无人写「真机 QA 已启用」——如实。

**跨链（承接，不在本链）**：P1-4 的 8×P3＋复核二 5×P3；P1-3 的 `QA-P13-P2-4-REAPPLY-MUTEX`＋若干 P3；P1-2 的 15×P3；P0-3 的 2×P2；`data/state.db` 真源待核；真实 whisper／真实 Obsidian 库／长视频耗时未跑。

## 十三、对「本链 supervisor 复检行尚未记」的裁定

**不算缺口，不打断口。** 依据：① 体例上 supervisor 行由 TM 在我出结论后追加（P1-3／P1-4 完全同款，P1-4 复检节亦作同款裁定）；②本链**业务 5 派**在 `DISPATCH-LOG` 已**逐行齐备**、与 HANDOFF／`TASK-MODEL-LOG` 三处**0 不匹配**，缺的只是「记录本次复检」这一行，**不影响任何对账**；③ `TASK-MODEL-LOG` 本链任务级行（L52，`rework=0`）已落盘并经我校验。**要求 TM 在本节落盘后追加 `DISPATCH-LOG` 的 P1-5 supervisor 行**（`role=supervisor`／`model=opencode-go/muse-spark-1.3-contributor`／`used=主`／`runtime=本窗口`／`result=PASS`）。

## 十四、结论

**PASS｜无 blocking｜本链 supervisor 累计打回 0/2。放行收口。**

- 三处对账 **0 不匹配**；两账本 **exit 0**（52／68）＋坏行负控 **9 类全 exit 1**。
- 三套自测 **482／157／58 rc=0**；`git diff --check` rc=0；8765 无监听。
- 【12 项】全部真闭环；【三铁律】①②③④ 判定同§五：**②③ 无机械保证、④ 私有层无上限 = P3 既有缺口、不阻塞**；本链 `.trim()` **未进落盘链路**（前端 `data-vocdel`／`esc(e.wrong)` 原样、后端 `server.py` 全文件 sha256 逐字一致）。
- 独立探针 **后端 21/21 ＋ 前端 36/36 rc=0**；真 Chrome 真实 DOM 行为取证（勾选不折叠／折叠三路可达）与 code-reviewer 结论一致。
- 抽检反向证伪 **6/6 rc=1**、4/4 文件**全量 sha256 还原**（未用 `git checkout`）。
- Phase Integrity **五查全过**；红线**逐项过**（未提交／无 secrets／未碰封存／`docs/sop/` 不存在／测试只落 tmp＋`assert_tmp` 时序正确／未写真实词库或 `data/`／主题浅色／**D-12 未放宽**／No-Clobber／`server.py` 零改动）。
- **P0／P1-2／P1-3／P1-4 已收口语义一行未碰**（后端以 sha256 全等证明，前端以 diff 0 命中 ＋ 157/157 证明）。

心跳：目标＝P1-5 全链收口复检｜剩 P0＝0｜下一步＝交 TM 补 supervisor 账本行并推进 P1-7。
