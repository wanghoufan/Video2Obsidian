# BUGS

| Bug ID | Priority | Stage P0 Blocking? | Repro | Status | Current Task | 备注（截图/日志一句） |
|---|---|---:|---|---|---|---|
| P1-9-QA-01 | P2 | 否 | 转写途中创建同名笔记，队列与诊断查看同一 run | OPEN | P1-9 | 队列保留 `PUBLISH_BLOCKED`/failed，诊断展示为 `SKIPPED`；两处口径相反 |
| P1-9-QA-02 | P3 | 否 | SKIPPED 详情/结果查看路径 | OPEN | P1-9 | verdict 仍可能包含目标绝对路径；与既有 PUBLISHED 同口径，建议跨链统一收紧 |
| P1-9-QA-03 | P3 | 否 | SKIPPED 行查看颜色 | OPEN | P1-9 | 前端非失败态落 `st-ok`，与成功同色；静态自测通过但未真机目检 |
| P1-9-QA-04 | P3 | 否 | 源文件长停顿期间目标已有半截同名稿 | OPEN | P1-9 | 门 0 先于源稳定门，可能直接 SKIPPED；本次未实测，属已知限制叠加 |
| P1-9-QA-05 | P3 | 否 | `PENDING_PUBLISH`、`BLOCKED_OUTPUT_FS/ROOT` 分支失败 | OPEN | P1-9 | 仍可能使用旧的“检查笔记库权限”文案；本次未改动这些分支 |
| P1-9-QA-06 | P3 | 否 | 目标 `x.md` 是目录而非文件 | OPEN | P1-9 | `isfile` 不命中后走入库错误，用户得到权限类文案；非本次新增 |
| P1-9-QA-07 | P3 | 否 | 重复执行 watcher shutdown 时序测试 | OPEN | 历史挂账 | 15k `is_running()` 偶发时序 flaky；本次 contract 因沙箱 loopback 限制未到该段，不能据此判本次引入 |

## 测试范围与环境

- 被测提交：`3d501a5`，基线 `378eac2`。
- 全部夹具使用外置临时目录与合成视频数据；调 handler 前已断言 data root 位于临时目录；未访问用户真实目录。
- 未修改 `app/`、`src/`、`tests/`，未 commit、未 push。
- HTTP 真起服务未覆盖：当前 Codex 沙箱禁止 loopback bind，contract 在 `ThreadingHTTPServer` bind 处以 `PermissionError` 中止；按任务约定记为环境限制，不判业务 FAIL。

## 独立复现结果

独立临时夹具共 13/14 条原始检查通过；唯一失败是测试脚本把“无命中返回 `None`”误写成必须为 `False`，复核后确认不是产品失败。更正语义后主路径与边界均通过。

### 主路径

1. 已有 `exists.md`：`state=SKIPPED`、`whisper_calls=0`，stub 调用列表为空。
2. 既有笔记 SHA-256 前后一致，未覆盖。
3. job 仅生成 `manifest.json`，未生成 `raw/`、`asr/`。
4. `_scan_disk_states(data_root)` 重读后仍返回 `SKIPPED`，证明内存清空/重读磁盘不会回落排队中。
5. 无同名 `fresh.mp4`：`state=PUBLISHED`，引擎 stub 调用 1 次，实际生成 `fresh.md`。

证据：`app/server.py:4837-4871`（门 0 与轻量 receipt）、`:4917`（引擎调用位于门后）、`:950-1010`（磁盘状态）、`:1040-1107`（命名真源与文件判定）。

### 边界与回归

- 子目录同名：目标按子目录解析，不会被根目录同名笔记误伤。
- vault 未配置：不会命中已有笔记判定。
- `.MP4`：可按单扩展名规则识别对应 `.md`。
- 同名目标为目录：`isfile` 不命中，不冒充“已有笔记”。
- PUBLISH_BLOCKED、PENDING_PUBLISH、BLOCKED_OUTPUT_FS/ROOT 的既有分支仅做静态核对，未覆盖 HTTP 真服务与真实 whisper。

## 规定自测记录

| 命令 | rc | 断言/结果 |
|---|---:|---|
| `.venv/bin/python tests/selftest_p1_2_contract.py` | 1 | 在 HTTP loopback bind 处被环境 `PermissionError` 中止；未取得完整 600 断言结果 |
| `.venv/bin/python tests/selftest_p1_2_frontend.py` | 0 | 全 PASS；含 S13 全部 SKIPPED 静态/桩断言 |
| `.venv/bin/python tests/selftest_v26_presets.py` | 0 | 58 全 PASS |

## 对 code-reviewer 结论的独立判断

- P2-1：同意，OPEN。`PUBLISH_BLOCKED` 在队列计 failed，但 `PUBLISH_TARGET_EXISTS` 在诊断展示为 SKIPPED；数据层保留该状态有利于重试，但用户口径不一致。
- P3-1：同意，OPEN。SKIPPED verdict 可能出路径；与旧 PUBLISHED 同口径，宜统一治理。
- P3-2：同意，OPEN。静态自测确认不画失败红，但当前成功色与跳过色相同；需产品决定是否改中性色。
- P3-3：同意，OPEN。门序存在半截稿叠加风险，本次未覆盖该时序场景。
- P3-4：同意，OPEN。非同名状态仍有旧权限文案，未被本提交覆盖。
- P3-5：同意，OPEN。同名目录不满足 `isfile`，后续错误文案仍可能像权限故障。
- P3-6：同意，OPEN/历史挂账。属于 watcher 时序 flaky 与依赖声明缺失，不归因于 P1-9。

## 结论

**PASS（无新增 P0/P1 业务 BUG）**。P1-9 主路径、No-Clobber、whisper 零调用、磁盘重读状态、正常发布及主要边界均通过；P2-1 与 P3-1～P3-6 保留为 OPEN/backlog。HTTP 段因环境限制未覆盖，真实 whisper 与真机 UI 目检未覆盖。

---

# supervisor 复检节（2026-09-15/16，本链复检；原文结论不动）

- 复检对象：commit `3d501a5`，基线 `378eac2`，DEVELOP-P1-9。
- 复检者：supervisor（本窗口执行；**实派模型与本仓 `USER_MODEL_OVERRIDE.md` 的 supervisor 行 `opencode-go/muse-spark-1.3-contributor` 不一致**，见 §五.3 挂账，换模型由用户定，不自作主张）。
- 红线遵守：全程 `app/`、`src/`、`tests/` **零写入**（`git diff --stat -- app src tests` 为空）；未 commit、未 push；夹具全部 `tempfile.mkdtemp` 外置（`/tmp/sup_p19_*`、`sup_mut_*`、`sup_retry_*`），跑完 `rmtree`；未碰 `~/Downloads/需转录视频`、`~/Downloads/暂不转录视频`、`~/Documents/ob`；**未用 `git checkout`** 还原任何文件（负控还原只用 `/tmp` 备份 `cp` 回写并验 sha256）。
- **结论：PASS 放行，打回 0/2。P0 ×0／P1 ×0。**

## 一、reviewer 与 qa 结论复核（抽查，不盲信）

**1.1 三套规定自测本窗口实测（本人跑，非转述）**

| 套件 | rc | 结果 |
|---|---:|---|
| `.venv/bin/python tests/selftest_p1_2_contract.py` | **0** | **600/600 全 PASS**（`SELFTEST ALL PASS（600 项断言）`）；15k 三条（`stop()` 如实返回 False／`is_running()` 仍为 True／一遍跑完后转 False）**本次全部 PASS，未复现 flaky** |
| `.venv/bin/python tests/selftest_p1_2_frontend.py` | **0** | 全 PASS（173 条 `PASS`，含 S13 全部 SKIPPED 断言与其反向证伪） |
| `.venv/bin/python tests/selftest_v26_presets.py` | **0** | 58 全 PASS |

→ qa 报告里「contract rc=1」确认为 **codex 沙箱禁 loopback bind 的环境限制**，本窗口可 bind 且**全过**，编排者补位结论成立。

**1.2 自建独立夹具抽验（自写断言，15/15 PASS，跑两遍一致）**

夹具：`.venv` ＋ `tempfile.mkdtemp`；真 `stage2.instance.acquire`＋`init_db`；真 `stage5.watcher.Watcher._on_fs_event` 投递；引擎换计数 stub（只数调用，不跑 whisper）。

| # | 断言 | 结果 |
|---|---|---|
| 1a | `same.mp4` 目标笔记已存在 → `state==SKIPPED` | PASS |
| 1b | **引擎真零调用**（stub 计数 `[]`，不是看文案） | PASS |
| 1c | `whisper_calls==0` | PASS |
| 1d | **既有笔记 sha256 前后一致** | PASS |
| 1e | 不建 `raw/`、不建 `asr/` | PASS |
| 1f | `_scan_disk_states` 重读仍 `SKIPPED`（重启不回落排队中） | PASS |
| 1g | `SKIPPED not in FAIL_STATES` | PASS |
| 2a–2d | 无同名 `fresh.mp4` → 真 `PUBLISHED`、引擎调 1 次、vault 真出 `fresh.md`、`same.md` 仍未被覆盖（**证明门 0 不误伤正常发布**） | 全 PASS |
| 3a | `queue.skipped>=1`（单列计数） | PASS |
| 3b | **`queue.failed==0`（SKIPPED 未污染 failed）** | PASS |
| 3c | `total == done + failed`（**skipped 不计入 total**） | PASS |
| 3d | `processed_n` 含 skipped | PASS |

**1.3 附加抽验（reviewer/qa 未单列的一条）**：删掉库里那篇笔记后对**同一 run** 重试 → `state=PUBLISHED`、引擎调 1 次、`vault/again.md` 真落盘。**门 0 不是永久锁死**，与前端「删除或改名后点重试（whisper 会跑）」文案自洽（rc=0）。

→ reviewer 的 51/51 与 qa 的主路径 5 条，本人独立复算**成立**。

## 二、反向证伪（本人自挑变异，5/5 全有牙）

| # | 变异做法 | 实测（证明哪条断言有牙） | 判定 |
|---|---|---|---|
| M1 | 摘掉门 0（`_vault_note_already_there` 恒回 None） | 基线 `SKIPPED`／零调用 → 变异后 **引擎真被调**（`calls=['same.mp4']`）、`PUBLISH_BLOCKED` | 有牙 |
| M4b | 命名真源指错（`_vault_publish_target` 恒指已存在的 `same.md`） | 无同名的 `fresh.mp4` 被**误判 SKIPPED 且引擎 0 次**（误伤方向） | 有牙 |
| M5 | 把 `SKIPPED` 塞进 `FAIL_STATES` | `failed` 由 0 → **1**、「skipped 不进 failed」有牙 | 有牙 |
| M2 | 状态码族**放宽**（加 `BLOCKED_OUTPUT_FS`） | `BLOCKED_OUTPUT_FS` 由 False → **True**（真权限问题会被冒充成「已有同名」） | 有牙 |
| M3 | 状态码族**收紧**（删 `CANONICAL_OUTPUT_EXISTS`） | 由 True → **False**（漏收方向） | 有牙 |

> 与 reviewer 的 6/6 互为独立印证（M1/M2/M3/M4b/M5 本人重跑，M6「磁盘不认 SKIPPED」由 1f 覆盖同一口径）。合计**证伪有牙 5/5（本人）＋6/6（reviewer）**。

## 三、越界判定：AST/逐函数对账（**未越界**）

脚本对 `378eac2` 与 `3d501a5` 的 `app/server.py` 逐顶层定义取源码段 sha256：

- 顶层条目 **161 → 165**；**新增 4**：`_manifest_last_receipt`、`_pub_target_exists`、`_vault_note_already_there`、`_vault_publish_target`；**删除 0**；**改动 7**：`_scan_disk_states`、`_listener_snapshot`、`_diag_action`、`_diagnosis_item`、`_handle_failure_diagnosis`、`_stage_text_zh`、`_process_one_run` —— 与 reviewer 报告**逐项一致**，7 个改动全在 P1-9 声明范围内。
- 点名核验**逐字未动**：`_err_text` ✅、`_strip_paths` ✅、`_state_bucket` ✅、`_reapply_result_bucket` ✅、`_app_resolve_canonical` ✅、`_stale_source_reason` ✅、`_is_under_root` ✅、`_handle_retry_post` ✅、`_handle_status` ✅、`_attach_completed_view` ✅、`_transcribe_worker` ✅、`_handle_clear_post` ✅、`_worker_record` ✅、`_diag_redact_path` ✅。
- **被删/被改的旧行全仓仅 6 行（server.py）**：均为 skipped 计数、`display`/`confidence` 分支、PUBLISH_BLOCKED 兜底文案、`job_dir` 计算行上移（语义等价，供门 0 复用）。`index.html` 旧行 9 条全在 skipped 文案/队列/toast/重试入口范围。
- **两道发布门零改动**（转写前源快照门、写库前门在 diff 中无任何 `-` 行）；`src/`（含 `watcher.py`、`reconcile.py`）**零命中**，`git diff --name-only 378eac2 3d501a5 -- src/` 输出 0 行。
- 前端 `isFailedRun` 仍只认 `FAIL`/`PUBLISH_BLOCKED` → **SKIPPED 不进批量重跑**（S13 静态断言与源码一致）。

## 四、账本校验（**真跑，看 exit 码**）

| 项 | 命令/做法 | 结果 |
|---|---|---|
| 正控 | 逐行 `json.loads` ＋ 行数核对 | **TASK 58 行／DISPATCH 95 行，坏行 0，EXIT=0** ✅ |
| 负控 | 向**真文件**末尾各塞 2 种坏行（JSON 截断／非 JSON），再跑同一校验 | **EXIT=1／1／1／1，共 4/4 有牙** ✅；随后用 `/tmp` 备份 `cp` 回写还原（**未用 `git checkout`**），sha256 与原始 `e3c47fd7…`／`828491ee…` **逐字一致**，还原后正控回 EXIT=0 |
| schema 校验（AGENTS 枚举） | 全字段＋`result`/`escalated`/`used` 枚举＋`rework` 整型＋role 属 9+1 | **EXIT=1，1 条不合规**：`TASK-MODEL-LOG.jsonl:40` `role="迁移整理工"` 不属 9+1（迁移期遗留）。`DISPATCH` 全行合规、`used` 恒「主」 ✅ |

## 五、挂账（非阻塞，逐条给证据）

1. **账本卫生**：`TASK-MODEL-LOG.jsonl:40` role 不合规；另 58 行中 **56 行 role=builder**（仅 1 行 qa、1 行迁移整理工），跨项目统计时角色维度会偏 —— 历史遗留，非 P1-9 引入，建议 neat-freak 收尾时规范化或加注记。
2. **DISPATCH 的 note 里写了 `tokens 82819`**：AGENTS 规定 DISPATCH 不记 tokens/cost（只在 note 文本里，未建字段），建议后续别写进 note，避免误当字段。
3. **实派 vs 表**：本链 supervisor 由本窗口模型执行，与 override 表 `opencode-go/muse-spark-1.3-contributor` **不一致**；P1-9 的 reviewer（`opencode/muse-spark-1.3-contributor-free`）与 qa（`codex/gpt-5.6-luna`，runtime=codex）**与表一致**。请编排者记 DISPATCH 时如实标注，是否改表由用户定。
4. **8899 上有 22:15 起的老实例（PID 89041，`.venv/bin/python app/server.py`）**，跑的是 `3d501a5` **之前**的代码 —— 若要真机复验 P1-9 必须先重启，别拿它验。

## 六、P2/P3 定级裁定

| 项 | 我的裁定 | 是否阻塞放行 | 理由与建议 |
|---|---|---|---|
| **P2-1**（队列计 failed vs 诊断显示 SKIPPED，口径相反） | **同意 P2，维持** | **不阻塞** | 数据层无害（初稿保留、未覆盖、可重入库），仅「这条算不算失败」的用户口径矛盾；且**只命中「转写途中才冒出同名笔记／重试入库撞名」这一支**，主路径（门 0 拦下）队列与诊断同为 SKIPPED、本就一致。**修法建议选 ②不选①**：把 `PUBLISH_TARGET_EXISTS` 从 `_NOTE_EXISTS_ROOTS` 摘掉（一行），展示态回 `PUBLISH_BLOCKED`、人话文案仍保留「已有同名笔记，未覆盖（不算失败）」；选 ①把撞名单列 skipped 会让「初稿已保留待重入库」这条关键信息在队列里消失。属 A 类小改，**可本链顺手修，不修也放行**——口径最终由用户拍。 |
| P3-1 脱敏：SKIPPED verdict 含绝对路径 | 同意 P3 | 否 | 与既有 PUBLISHED verdict 同口径，非新增；本机单人工具，路径只回给本人。要收紧就与 PUBLISHED 一起改，属跨链，需用户点头。 |
| P3-2 已跳过与成功同色 | 同意 P3 | 否 | 纯观感；**建议本窗口补一次真机目检**（本链未做浏览器目检），目检前不作为结论。 |
| P3-3 门 0 早于源稳定门（与半截稿限制叠加） | 同意 P3 | 否 | 叠加后最差结果是「已跳过」而非「入库受阻」，无新增数据风险，与既有已知限制同源。**可选优化**：把门 0 移到源稳定门之后（一行位置），成本极低即可消除叠加；做不做由编排者定，不做不卡。 |
| P3-4 非同名族仍用旧「检查笔记库权限」文案 | 同意 P3 | 否 | 本提交只改了「同名」一支；注意 `_stage_text_zh` 兜底文案已改（`:`→「看本地证据确认原因后点重试入库」）而 `_process_one_run` 末段 else 分支未改，两处兜底现有轻微措辞不一致，建议一并统一。 |
| P3-5 同名目标是目录 | 同意 P3 | 否 | `isfile` 不命中后落入库错误，非本次新增。 |
| P3-6 历史挂账（15k flaky ＋ 无依赖声明文件） | 同意挂账 | 否 | 本次 contract 15k **三条全 PASS、未复现**，与 P3-e 同源继续挂账。**「无依赖声明文件」建议 Windows 迁移链开工前按 P2 对待**（新机器必踩），不卡 P1-9。 |

## 七、工作树卫生与环境红线

- 工作树除**报告与账本**外无业务改动：`app src tests` 的 `git diff --stat` **为空**；未提交项仅 `M docs/model/DISPATCH-LOG.jsonl`、`M docs/model/TASK-MODEL-LOG.jsonl`（编排者的 P1-9 派工行，非本人新增）、`?? docs/qa/P1-9-SKIP-QA-2026-09-15.md`、`?? docs/review/P1-9-SKIP-EXISTING-NOTE-CODE-REVIEW.md`。
- `.codebuddy/`：会话产物，**不提交不删** ✅。
- `__pycache__`：11 处（`app/`、`tests/`、各 `src/stage*/`）与根 `.DS_Store` —— **只列不删**，且 `.gitignore` 均已覆盖，不影响提交；若要清理请先报用户/交 neat-freak。
- **8765 红线**：`lsof` 实测 8765 仍由 **PID 6586**（另一项目 python3.13）监听，**未被 bind、未被 kill** ✅。
- 用户真实目录（`~/Downloads/需转录视频`、`~/Downloads/暂不转录视频`、`~/Documents/ob`）**全程未访问** ✅。

## 八、supervisor 结论

**PASS，放行推送，打回 0/2。** P0 ×0／P1 ×0；P2-1 与 P3-1～P3-6 **全部同意现有定级**（无升级、无降级），均**不阻塞**。放行附加条件：**无硬条件**；三条建议（P2-1 按建议②可选顺手修；Windows 链开工前先补依赖声明；真机 UI 目检补一次）由编排者/用户决定是否并入。

---

## 收尾注记（neat-freak，2026-09-16；只加注，不改正文、不改结论）

- 本节 `:134` 记「DISPATCH **95** 行」，当前工作树实测 `docs/model/DISPATCH-LOG.jsonl` 为 **96 行**（基线 `378eac2`＝93，`e38151a` 新增 reviewer／qa／supervisor 三行 ＝ +3）。差 1 行＝记账时刻口径（校验在最后一行落盘前），**非坏行**，「坏行 0／EXIT=0」结论不变。差异明细与其余核对项见 `docs/handoff/HANDOFF.md` §一.4 尾部「收尾注记」。
- 本轮 neat-freak 另已办历史未决项：`docs/pm/STAGE5-PLAN.md:47` 与 `docs/qa/STAGE5-QA-REPORT.md:24/:52/:65` 的 A5 口径（P1-1-FIX 后首投为 `PROMOTED`）已在两文件尾部各加注记，正文未动。
