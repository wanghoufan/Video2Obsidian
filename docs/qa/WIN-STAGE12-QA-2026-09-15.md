# BUGS

| Bug ID | Priority | Stage P0 Blocking? | Repro | Status | Current Task | 备注（截图/日志一句） |
|---|---|---:|---|---|---|---|
| WIN-STAGE12-QA-001 | P0（**已撤销**） | 否（裁定不成立） | 执行 `git status --short` | **已撤销** | Windows Stage 1/2 QA | 原判「隔离铁律越界」；supervisor 复检裁定**不成立**：`git status --short -- app src tests` 与 `git diff --stat -- app src tests` 双双为空（仓根 Mac 端业务代码零改动），本条所指三项（派工账 DISPATCH-LOG／reviewer 报告／`.codebuddy/` 会话产物）均非业务改动，且 QA 报告自身亦在 `docs/qa/`，该口径自相矛盾。属任务书口径误判，非 P0。 |

## QA 结论

- 新增业务 BUG：**0**。
- 结论：**PASS**（原 FAIL 仅由 QA-001 一条导致；该条经 supervisor 复检裁定不成立并撤销，业务侧零 BUG、隔离铁律实测成立）。
- 测试仅使用 `/tmp` 或系统临时目录的合成数据；未访问用户真实目录，未绑定/关闭 8765，未修改业务代码。

## 独立复现

- 独立纯 Python 夹具：17/17 PASS，覆盖 259/260/261、大小写/盘符/UNC/中文空格丨、reveal 逗号/引号、LOCALAPPDATA、三根接受前闸、Whisper 前闸。
- 独立假 msvcrt 锁夹具：3/3 PASS；第一句柄保持打开、第二句柄被非阻塞 1-byte 锁拦截、调用参数为 `(LK_NBLCK, 1)`。
- 反向证伪：3/3 有牙（放大长度上限、包含判定恒真、去掉 reveal 降级均改变结果）。

## 四套回归

| 自测 | rc | 断言/结果 |
|---|---:|---|
| `selftest_p1_2_contract.py` | 1 | HTTP 合约阶段绑定 `127.0.0.1:0` 被 macOS 沙箱拒绝；此前 245 条 PASS，未达到完整 600 条。 |
| `selftest_p1_2_frontend.py` | 0 | FRONT ALL PASS、BRAND/LAYOUT PASS；脚本未输出统一总数（176 条 PASS 行，含 2 条 banner）。 |
| `selftest_v26_presets.py` | 0 | 58 条断言 PASS。 |
| `selftest_win_stage12.py` | 0 | 138 条断言 PASS，反向证伪 13/13。 |

`compileall` 也受 macOS 沙箱禁止写 Python 缓存影响而 rc=1，非源码语法错误证据；此前 Windows 自测已覆盖导入/执行路径。

## Reviewer P2/P3 独立判断

- P2-1：**反对继续保留，已解决**。`server.py:5451` 接受前、`:4890` Whisper 前、`server.py:453` 归属判定、`candidate.py:152` discover 已接线；独立夹具验证前三处，Windows 自测验证 discover。
- P2-2：**同意**。UNC 共享根自身 `is_within` 返回 False，方向 fail-closed；当前入口又以 `BLOCKED_UNC_ROOT` 拦截，暂不造成越权放行，列真机复验。
- P3-1：**同意挂账**。`start.ps1` 与 `server.py` 各持默认端口字面量，当前启动链自测通过。
- P3-2：**同意挂账**。watcher 遗留 `os.open/os.close` 无功能影响，随 watcher 下轮清理。
- P3-3：**同意挂账**。平台常量为 import 期快照；真 Windows 运行无影响，桩测需 reload。
- P3-4：**同意挂账**。哈希是占位；Windows cp312 wheel 可得性需真机/联网环境验证。
- P3-5：**同意挂账**。`windows/app/start.sh` 是 Mac 副本，整目录交付会造成误导。
- P3-6：**同意观察项**。本批仅要求网络盘阻断；非 NTFS 本地盘是否阻断需产品决策。

## 环境限制与待补

macOS 无法真实验证 msvcrt 跨进程互斥、CreateFileW 占用、Windows 卷 API、Explorer、PowerShell、长路径策略注册表和真实 faster-whisper/CT2 wheel；这些均不判业务 FAIL，交 Windows 11 真机补测。

---

# supervisor 复检节（追加，上方原文一字未动）

- 角色：supervisor（监督者）｜日期：2026-09-16｜对象：`windows/**` Stage 1 启动链 + Stage 2 文件安全
- 本轮：**未改任何代码**（含 `windows/` 内）、未 commit、未 push；测试只用 `/tmp` 合成数据；未触碰用户真实目录；未 bind / 未 kill 8765 与 PID 6586。
- **裁定：放行（打回 0/2）** —— P0 × 0、P1 × 0；新增 P3 × 1（观察项）。

## S-0｜QA-001（隔离铁律 P0）裁定：**不成立，予以撤销**

不盲从任何一方，独立核两条硬证据：

1. `git status --short`（本轮实测，`main` HEAD `ebabf91`）：改动＝`M windows/**` ×9 + `?? windows/**` ×5 + `M docs/model/DISPATCH-LOG.jsonl` + `?? docs/review/WIN-STAGE12-CODE-REVIEW.md` + `?? docs/qa/WIN-STAGE12-QA-2026-09-15.md` + `?? .codebuddy/`。
2. `git diff --stat -- app src tests` **与** `git status --short -- app src tests` **双双为空** → 仓根 Mac 端业务代码（`app/ src/ tests/`）**零改动、零新增**（已冻结 tag `v1.0-mac` 未被带歪）。

对照隔离铁律原文（`docs/handoff/HANDOFF.md` §一.6）：铁律约束的是「**Windows 施工只改 `windows/**`，仓根 `app/ src/ tests/` 零改动**」，即**业务改动边界**；三项被 QA 计入越界的文件均非业务改动：

| 项 | 性质 | 依据 |
|---|---|---|
| `docs/model/DISPATCH-LOG.jsonl`（+3 行） | 派工账，非业务 | AGENTS「逐派记录…每次派工收工编排者往 DISPATCH-LOG 记一行」；实测新增行＝builder首版／code-reviewer／builder返工三行，`git diff -U0` 逐行可见，**纯新增、无删改历史行** |
| `docs/review/WIN-STAGE12-CODE-REVIEW.md` | reviewer 法定落点 | AGENTS 谁写哪表：code-reviewer → `docs/review/` |
| `.codebuddy/` | 会话产物，规定保留不提交 | HANDOFF §一.6 第 4 条排除项；untracked 即未提交 |

**反证（一句话）**：本 QA 报告自身也落在 `docs/qa/` 而非 `windows/**`——若按 QA 的口径，本报告即第一号越界物，口径自相矛盾，故判为**任务书口径误判**，不是 P0。

**建议（不阻塞）**：给 `.gitignore` 补一行 `.codebuddy/`，让会话产物不再出现在 `git status`，从根上消掉这类误判。BUG 表 `WIN-STAGE12-QA-001` 行建议由 TM/neat 标注「已撤销（口径误判）」，本复检节不改动上方原文。

## S-1｜核心断言抽查（自建夹具，不复用 builder 断言）

夹具：`/tmp/sup_win_stage12/indep_sup.py`（真 `_handle_start_post`／真 `_process_one_run`／真跨进程 `stage2.instance`，非 mock 内部）。**断言 22，失败 0，rc=0。**

| # | 抽查项 | 实测 |
|---|---|---|
| 1 | 锁：跨进程第二实例 | 第一实例真持锁 → 第二进程 `python -m stage2.instance` **rc=3**、stderr 含 `SECOND_INSTANCE`；DB（`data/state.db`）mtime_ns+size 指纹前后**完全一致**（`1789494881850630155, 102400`）＝不写 DB |
| 2 | 三根接受前闸 | 可落地超长根（352 字符）→ **400 + `code=BLOCKED_WIN_ROOT_PATH`**，人话含「太长」，响应体**不含真实绝对路径**；`os.makedirs` 未发生（`land` 与 `land/data` 均 `exists=False`）＝**零写盘** |
| 3 | 送 Whisper 前闸 | 真 `_process_one_run`（DB 造 QUEUED/AUTO run + 真 399 字符源文件）→ `state=FAIL`、`whisper_calls=0`、**被 spy 的 `_transcribe_audio` 调用 0 次**、job 目录未创建 |
| 4 | 归属判定 | `_is_under_root` 真观测到委托 `platform_win.is_within` 1 次 |

补充：编排者补位的 `windows/tests/selftest_win_stage12.py` **138 断言 rc=0、证伪 13/13 有牙**（本轮复跑一致）；`selftest_p1_2_contract.py` 600/600 由编排者本窗口补位成立（qa 因沙箱禁 bind 只到 245，属环境限制非代码缺陷）。

## S-2｜反向证伪抽查：**3/3 有牙**（加总：本轮 3 + qa 3 + 自测 13 + reviewer 10）

| 变异 | 改前 → 改后 |
|---|---|
| `platform_win.USABLE_PATH_LIMIT` 259→4096 | 超长根 400 + `BLOCKED_WIN_ROOT_PATH` → 不再被拦 |
| `server._win_path_too_long` 恒 `None` | 超长源 `FAIL/whisper_calls=0/太长` → 放行到 ffmpeg 才失败（证明闸才是拦点） |
| `platform_win.is_within` 换成朴素 `startswith` | 「自身/边界」归属语义失真，断言变红 |

（注：第 2 条我第一版探针写成「FAIL 且 whisper_calls=0」，变异后因 ffmpeg 失败仍绿＝**假有牙**，已把探针收紧为「verdict 含『太长』」重跑，判定为真有牙。记此以免后续复用时踩同一个坑。）

## S-3｜越界与误伤

- **范围**：9 改（server/ingest/launch_plist/reliability/menu_bar/candidate/instance/volume_probe/watcher）+ 5 新增（platform_win.py 638／start.ps1 59／start.bat 6／requirements.txt 25／自测 1028），**逐 hunk 人工过目**，全部落在「Stage 1 启动链 + Stage 2 文件安全」声明范围（去 POSIX 化、卷/锁/占用/长路径/路径比较/reveal/data root/启动脚本/依赖），未夹带 Stage 3+ 或 Mac 端语义改动。
- **watcher 稳定门**：`DEBOUNCE_S=3.0 / STABLE_PROBE_S=2.0 / STABLE_ROUNDS=3 / BUSY_CHECK=True`，与 Mac 端 `src/stage5/watcher.py:84-87` **逐项相等**（改了即 P0，未改 ✅）。
- **平台分支单点**：`windows/src/**`、`windows/app/**` 内 `sys.platform/os.name/platform.system` 命中**仅 `platform_win.py` 自身**（外加自测桩），业务侧零命中 ✅。
- **长路径红线**：`platform_win.py` 与 `start.ps1`/`start.bat` 中 `SetValueEx/CreateKey/DeleteKey/SetValue/RegSetValue/runas/Verb` **全零命中**；注册表只读 `OpenKey/QueryValueEx`，读不到→`None`→BLOCK；`start.ps1` 端口占用只报 PID、明确不杀进程、不开 `0.0.0.0` ✅。

## S-4｜账本（看 exit 码，/tmp 备份，未用 `git checkout`）

| 项 | 命令 | exit |
|---|---|---|
| 正控 TASK（58 行） | `python3 validate_jsonl.py docs/model/TASK-MODEL-LOG.jsonl` | **0**（bad=0，sha `e3c47fd7…`） |
| 正控 DISPATCH（99 行） | 同上 | **0**（bad=0，sha `7e7253a3…`） |
| 负控 TASK（塞坏行） | 同上（/tmp 副本 +1 坏行） | **1**（`BAD line 59`） |
| 负控 DISPATCH（塞非 JSON 行） | 同上 | **1**（`BAD line 100`） |

仓内两文件**未被改动**：负控只在 `/tmp` 副本上做；事后 `diff -q` 与 `/tmp/sup_backup_*.jsonl` 一致、sha 与正控时相同。schema 抽查：TASK 58 行缺字段 0、无 `_example` 残留；DISPATCH 99 行缺字段 0，`used` 恒「主」。

**待补（编排者收口时办，不阻塞）**：TASK 账本尚无 WIN-STAGE1+2 行（应补 `result=PASS, rework=1, escalated=NO`）；DISPATCH 账本已有 builder 首版／code-reviewer／builder 返工三行，**尚缺 qa（codex/gpt-5.6-luna）与 supervisor（本轮）两行**。

## S-5｜红线

- 8765：仍被外部项目 `python3` PID 6586 LISTEN，**未 bind、未 kill** ✅；8899 上有本项目遗留进程 PID 89041（2026-09-15 22:15 起，`.venv/bin/python app/server.py`）——**非本批引入、非红线项**，本轮同样未动，仅登记。
- 未读/未写用户真实目录（全部 `/tmp` 或 `tempfile.mkdtemp`）✅；未 commit、未 push ✅。

## S-6｜P2/P3 定级意见

- **P2-2（UNC 共享根 `is_within`=False）**：**同意维持 P2，不阻塞**。补充证据：UNC 根在入口已被 `validate_root → BLOCKED_UNC_ROOT` 拦（`server.py:5451` 三根闸 + `platform_win.validate_root`），故 P2-1 接线后 **UNC 根作为 input_root 实际不可达**；方向又是 fail-closed，不会越权放行。修法成本低（`is_within` 对 UNC 共享根加前缀兜底 + 补一条正向断言），建议**与 Stage 2 真机复验同批做掉**，期限＝Stage 3 动工前。
- **P3-1（8899 两份字面量）**：同意挂账。建议加一条读取 `server.py` 的 `PORT =` 行做一致性断言，成本极低。
- **P3-2（watcher 遗留 `os.open/os.close`）**：同意挂账，无功能影响，随下轮 watcher 改动清。
- **P3-3（常量 import 期快照）**：同意挂账，真机无影响，写桩需 `importlib.reload`。
- **P3-4（哈希占位 + wheel 可得性）**：同意挂账，但 **cp312 win-amd64 wheel 可得性升为「Windows 真机交付前必验项」**，不得推断通过。
- **P3-5（`windows/app/start.sh` Mac 副本）**：同意，且**同口径扩到 `windows/README.md`／`README.en.md`**（同为 Mac 文案副本，整目录交付同样误导）。**期限＝整目录拷出成新仓库之前必办**（删除或改名标注），不阻塞本批。
- **P3-6（非 NTFS 本地盘）**：同意为观察项，属产品口径，不阻塞；列入待拍板清单。
- **新增 P3-7（supervisor 提出，观察项，不阻塞）**：`stage2/candidate.py` 的 `_blocked_candidate` 每次 discover 都新插一行 `BLOCKED_PATH_TOO_LONG`（实测同一超长源连 discover 3 次 → 3 行候选 + 3 行 state_event；`sources=0`，fail-closed 正常）。**与既有 `SOURCE_MISSING` 同构**（实测缺失文件连 discover 3 次同样 3 行），非本批新引入的异类行为，故只记 P3；但若文件夹里长期留着一个超限文件，周期性 reconcile 会让该表持续增长。建议随 watcher/reconcile 下轮一并做「按 `path_identity_key` 复用已有 BLOCKED 行、只更新 `updated_at`」。

## S-7｜放行附加条件（5 条）

1. **QA-001 撤销**：BUG 表该行标「已撤销（口径误判）」，本批结论由 FAIL 改 **PASS**（新增业务 BUG 仍为 0）。
2. **账本补行**：TASK 补 WIN-STAGE1+2 行（PASS / rework=1）；DISPATCH 补 qa 与 supervisor 两行。
3. **真机清单不许推断通过**（交 Windows 11）：msvcrt 跨进程互斥、CreateFileW 占用语义、`GetVolumeInformationW`/`GetDriveTypeW`、Explorer `/select,`、PowerShell 实跑、长路径策略注册表读值、faster-whisper/CT2 cp312 wheel 可得性。
4. **P2-2 与 P3-5 有期限**：P2-2＝Stage 3 动工前；P3-5（含两份 README）＝整目录拷出前。
5. **遗留进程登记**：8899 上 PID 89041 为旧会话遗留，收口时确认其归属再处理，不得就地误杀。

**supervisor 结论：放行（打回 0/2）。**

